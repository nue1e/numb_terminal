"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const UiWalletHandleRegistry_js_1 = require("../UiWalletHandleRegistry.js");
describe('the wallet registry', () => {
    describe('given a handle against which a Standard wallet is registered', () => {
        let mockWallet;
        let uiWalletHandle;
        beforeEach(() => {
            mockWallet = {};
            uiWalletHandle = {};
            (0, UiWalletHandleRegistry_js_1.registerWalletHandle)(uiWalletHandle, mockWallet);
        });
        it('lets you recover a registered wallet by its handle', () => {
            const recoveredWallet = (0, UiWalletHandleRegistry_js_1.getWalletForHandle)(uiWalletHandle);
            expect(recoveredWallet).toBe(mockWallet);
        });
    });
    it('throws if there is no registered wallet pertaining to the supplied handle', () => {
        const unregisteredHandle = {};
        expect(() => {
            (0, UiWalletHandleRegistry_js_1.getWalletForHandle)(unregisteredHandle);
        }).toThrow();
    });
});
//# sourceMappingURL=uiWalletHandleRegistry-test.js.map