import { Kind as e, specifiedRules as t, NoUnusedFragmentsRule as i, ExecutableDefinitionsRule as n, validate as a, print as s, parse as l, GraphQLError as c, NoDeprecatedCustomRule as h, visit as f } from "graphql";

import { createHash as d } from "crypto";

var v;

function init(e) {
  v = e.typescript;
}

class CharacterStream {
  constructor(e) {
    this._start = 0;
    this._pos = 0;
    this.getStartOfToken = () => this._start;
    this.getCurrentPosition = () => this._pos;
    this.eol = () => this._sourceText.length === this._pos;
    this.sol = () => 0 === this._pos;
    this.peek = () => this._sourceText.charAt(this._pos) || null;
    this.next = () => {
      var e = this._sourceText.charAt(this._pos);
      this._pos++;
      return e;
    };
    this.eat = e => {
      if (this._testNextCharacter(e)) {
        this._start = this._pos;
        this._pos++;
        return this._sourceText.charAt(this._pos - 1);
      }
      return;
    };
    this.eatWhile = e => {
      var t = this._testNextCharacter(e);
      var i = !1;
      if (t) {
        i = t;
        this._start = this._pos;
      }
      while (t) {
        this._pos++;
        t = this._testNextCharacter(e);
        i = !0;
      }
      return i;
    };
    this.eatSpace = () => this.eatWhile(/[\s\u00a0]/);
    this.skipToEnd = () => {
      this._pos = this._sourceText.length;
    };
    this.skipTo = e => {
      this._pos = e;
    };
    this.match = (e, t = !0, i = !1) => {
      var n = null;
      var a = null;
      if ("string" == typeof e) {
        a = new RegExp(e, i ? "i" : "g").test(this._sourceText.slice(this._pos, this._pos + e.length));
        n = e;
      } else if (e instanceof RegExp) {
        n = null == (a = this._sourceText.slice(this._pos).match(e)) ? void 0 : a[0];
      }
      if (null != a && ("string" == typeof e || a instanceof Array && this._sourceText.startsWith(a[0], this._pos))) {
        if (t) {
          this._start = this._pos;
          if (n && n.length) {
            this._pos += n.length;
          }
        }
        return a;
      }
      return !1;
    };
    this.backUp = e => {
      this._pos -= e;
    };
    this.column = () => this._pos;
    this.indentation = () => {
      var e = this._sourceText.match(/\s*/);
      var t = 0;
      if (e && 0 !== e.length) {
        var i = e[0];
        var n = 0;
        while (i.length > n) {
          if (9 === i.charCodeAt(n)) {
            t += 2;
          } else {
            t++;
          }
          n++;
        }
      }
      return t;
    };
    this.current = () => this._sourceText.slice(this._start, this._pos);
    this._sourceText = e;
  }
  _testNextCharacter(e) {
    var t = this._sourceText.charAt(this._pos);
    var i = !1;
    if ("string" == typeof e) {
      i = t === e;
    } else {
      i = e instanceof RegExp ? e.test(t) : e(t);
    }
    return i;
  }
}

function opt(e) {
  return {
    ofRule: e
  };
}

function list(e, t) {
  return {
    ofRule: e,
    isList: !0,
    separator: t
  };
}

function t$1(e, t) {
  return {
    style: t,
    match: t => t.kind === e
  };
}

function p(e, t) {
  return {
    style: t || "punctuation",
    match: t => "Punctuation" === t.kind && t.value === e
  };
}

var isIgnored = e => " " === e || "\t" === e || "," === e || "\n" === e || "\r" === e || "\ufeff" === e || " " === e;

var g = {
  Name: /^[_A-Za-z][_0-9A-Za-z]*/,
  Punctuation: /^(?:!|\$|\(|\)|\.\.\.|:|=|&|@|\[|]|\{|\||\})/,
  Number: /^-?(?:0|(?:[1-9][0-9]*))(?:\.[0-9]*)?(?:[eE][+-]?[0-9]+)?/,
  String: /^(?:"""(?:\\"""|[^"]|"[^"]|""[^"])*(?:""")?|"(?:[^"\\]|\\(?:"|\/|\\|b|f|n|r|t|u[0-9a-fA-F]{4}))*"?)/,
  Comment: /^#.*/
};

var m = {
  Document: [ list("Definition") ],
  Definition(t) {
    switch (t.value) {
     case "{":
      return "ShortQuery";

     case "query":
      return "Query";

     case "mutation":
      return "Mutation";

     case "subscription":
      return "Subscription";

     case "fragment":
      return e.FRAGMENT_DEFINITION;

     case "schema":
      return "SchemaDef";

     case "scalar":
      return "ScalarDef";

     case "type":
      return "ObjectTypeDef";

     case "interface":
      return "InterfaceDef";

     case "union":
      return "UnionDef";

     case "enum":
      return "EnumDef";

     case "input":
      return "InputDef";

     case "extend":
      return "ExtendDef";

     case "directive":
      return "DirectiveDef";
    }
  },
  ShortQuery: [ "SelectionSet" ],
  Query: [ word("query"), opt(name$1("def")), opt("VariableDefinitions"), list("Directive"), "SelectionSet" ],
  Mutation: [ word("mutation"), opt(name$1("def")), opt("VariableDefinitions"), list("Directive"), "SelectionSet" ],
  Subscription: [ word("subscription"), opt(name$1("def")), opt("VariableDefinitions"), list("Directive"), "SelectionSet" ],
  VariableDefinitions: [ p("("), list("VariableDefinition"), p(")") ],
  VariableDefinition: [ "Variable", p(":"), "Type", opt("DefaultValue") ],
  Variable: [ p("$", "variable"), name$1("variable") ],
  DefaultValue: [ p("="), "Value" ],
  SelectionSet: [ p("{"), list("Selection"), p("}") ],
  Selection: (e, t) => "..." === e.value ? t.match(/[\s\u00a0,]*(on\b|@|{)/, !1) ? "InlineFragment" : "FragmentSpread" : t.match(/[\s\u00a0,]*:/, !1) ? "AliasedField" : "Field",
  AliasedField: [ name$1("property"), p(":"), name$1("qualifier"), opt("Arguments"), list("Directive"), opt("SelectionSet") ],
  Field: [ name$1("property"), opt("Arguments"), list("Directive"), opt("SelectionSet") ],
  Arguments: [ p("("), list("Argument"), p(")") ],
  Argument: [ name$1("attribute"), p(":"), "Value" ],
  FragmentSpread: [ p("..."), name$1("def"), list("Directive") ],
  InlineFragment: [ p("..."), opt("TypeCondition"), list("Directive"), "SelectionSet" ],
  FragmentDefinition: [ word("fragment"), opt(function butNot(e, t) {
    var i = e.match;
    e.match = e => {
      var n = !1;
      if (i) {
        n = i(e);
      }
      return n && t.every(t => t.match && !t.match(e));
    };
    return e;
  }(name$1("def"), [ word("on") ])), "TypeCondition", list("Directive"), "SelectionSet" ],
  TypeCondition: [ word("on"), "NamedType" ],
  Value(e) {
    switch (e.kind) {
     case "Number":
      return "NumberValue";

     case "String":
      return "StringValue";

     case "Punctuation":
      switch (e.value) {
       case "[":
        return "ListValue";

       case "{":
        return "ObjectValue";

       case "$":
        return "Variable";

       case "&":
        return "NamedType";
      }
      return null;

     case "Name":
      switch (e.value) {
       case "true":
       case "false":
        return "BooleanValue";
      }
      if ("null" === e.value) {
        return "NullValue";
      }
      return "EnumValue";
    }
  },
  NumberValue: [ t$1("Number", "number") ],
  StringValue: [ {
    style: "string",
    match: e => "String" === e.kind,
    update(e, t) {
      if (t.value.startsWith('"""')) {
        e.inBlockstring = !t.value.slice(3).endsWith('"""');
      }
    }
  } ],
  BooleanValue: [ t$1("Name", "builtin") ],
  NullValue: [ t$1("Name", "keyword") ],
  EnumValue: [ name$1("string-2") ],
  ListValue: [ p("["), list("Value"), p("]") ],
  ObjectValue: [ p("{"), list("ObjectField"), p("}") ],
  ObjectField: [ name$1("attribute"), p(":"), "Value" ],
  Type: e => "[" === e.value ? "ListType" : "NonNullType",
  ListType: [ p("["), "Type", p("]"), opt(p("!")) ],
  NonNullType: [ "NamedType", opt(p("!")) ],
  NamedType: [ function type$1(e) {
    return {
      style: e,
      match: e => "Name" === e.kind,
      update(e, t) {
        var i;
        if (null === (i = e.prevState) || void 0 === i ? void 0 : i.prevState) {
          e.name = t.value;
          e.prevState.prevState.type = t.value;
        }
      }
    };
  }("atom") ],
  Directive: [ p("@", "meta"), name$1("meta"), opt("Arguments") ],
  DirectiveDef: [ word("directive"), p("@", "meta"), name$1("meta"), opt("ArgumentsDef"), word("on"), list("DirectiveLocation", p("|")) ],
  InterfaceDef: [ word("interface"), name$1("atom"), opt("Implements"), list("Directive"), opt("FieldsDef") ],
  Implements: [ word("implements"), list("NamedType", p("&")) ],
  DirectiveLocation: [ name$1("string-2") ],
  SchemaDef: [ word("schema"), list("Directive"), p("{"), list("OperationTypeDef"), p("}") ],
  OperationTypeDef: [ name$1("keyword"), p(":"), name$1("atom") ],
  ScalarDef: [ word("scalar"), name$1("atom"), list("Directive") ],
  ObjectTypeDef: [ word("type"), name$1("atom"), opt("Implements"), list("Directive"), opt("FieldsDef") ],
  FieldsDef: [ p("{"), list("FieldDef"), p("}") ],
  FieldDef: [ name$1("property"), opt("ArgumentsDef"), p(":"), "Type", list("Directive") ],
  ArgumentsDef: [ p("("), list("InputValueDef"), p(")") ],
  InputValueDef: [ name$1("attribute"), p(":"), "Type", opt("DefaultValue"), list("Directive") ],
  UnionDef: [ word("union"), name$1("atom"), list("Directive"), p("="), list("UnionMember", p("|")) ],
  UnionMember: [ "NamedType" ],
  EnumDef: [ word("enum"), name$1("atom"), list("Directive"), opt("EnumValuesDef") ],
  EnumValuesDef: [ p("{"), list("EnumValueDef"), p("}") ],
  EnumValueDef: [ name$1("string-2"), list("Directive") ],
  InputDef: [ word("input"), name$1("atom"), list("Directive"), opt("InputFieldsDef") ],
  InputFieldsDef: [ p("{"), list("InputValueDef"), p("}") ],
  ExtendDef: [ word("extend"), "ExtensionDefinition" ],
  ExtensionDefinition(t) {
    switch (t.value) {
     case "schema":
      return e.SCHEMA_EXTENSION;

     case "scalar":
      return e.SCALAR_TYPE_EXTENSION;

     case "type":
      return e.OBJECT_TYPE_EXTENSION;

     case "interface":
      return e.INTERFACE_TYPE_EXTENSION;

     case "union":
      return e.UNION_TYPE_EXTENSION;

     case "enum":
      return e.ENUM_TYPE_EXTENSION;

     case "input":
      return e.INPUT_OBJECT_TYPE_EXTENSION;
    }
  },
  [e.SCHEMA_EXTENSION]: [ "SchemaDef" ],
  [e.SCALAR_TYPE_EXTENSION]: [ "ScalarDef" ],
  [e.OBJECT_TYPE_EXTENSION]: [ "ObjectTypeDef" ],
  [e.INTERFACE_TYPE_EXTENSION]: [ "InterfaceDef" ],
  [e.UNION_TYPE_EXTENSION]: [ "UnionDef" ],
  [e.ENUM_TYPE_EXTENSION]: [ "EnumDef" ],
  [e.INPUT_OBJECT_TYPE_EXTENSION]: [ "InputDef" ]
};

function word(e) {
  return {
    style: "keyword",
    match: t => "Name" === t.kind && t.value === e
  };
}

function name$1(e) {
  return {
    style: e,
    match: e => "Name" === e.kind,
    update(e, t) {
      e.name = t.value;
    }
  };
}

function onlineParser(t = {
  eatWhitespace: e => e.eatWhile(isIgnored),
  lexRules: g,
  parseRules: m,
  editorConfig: {}
}) {
  return {
    startState() {
      var i = {
        level: 0,
        step: 0,
        name: null,
        kind: null,
        type: null,
        rule: null,
        needsSeparator: !1,
        prevState: null
      };
      pushRule(t.parseRules, i, e.DOCUMENT);
      return i;
    },
    token: (e, i) => function getToken(e, t, i) {
      var n;
      if (t.inBlockstring) {
        if (e.match(/.*"""/)) {
          t.inBlockstring = !1;
          return "string";
        }
        e.skipToEnd();
        return "string";
      }
      var {lexRules: a, parseRules: s, eatWhitespace: l, editorConfig: c} = i;
      if (t.rule && 0 === t.rule.length) {
        popRule(t);
      } else if (t.needsAdvance) {
        t.needsAdvance = !1;
        advanceRule(t, !0);
      }
      if (e.sol()) {
        var h = (null == c ? void 0 : c.tabSize) || 2;
        t.indentLevel = Math.floor(e.indentation() / h);
      }
      if (l(e)) {
        return "ws";
      }
      var f = function lex(e, t) {
        var i = Object.keys(e);
        for (var n = 0; n < i.length; n++) {
          var a = t.match(e[i[n]]);
          if (a && a instanceof Array) {
            return {
              kind: i[n],
              value: a[0]
            };
          }
        }
      }(a, e);
      if (!f) {
        if (!e.match(/\S+/)) {
          e.match(/\s/);
        }
        pushRule(S, t, "Invalid");
        return "invalidchar";
      }
      if ("Comment" === f.kind) {
        pushRule(S, t, "Comment");
        return "comment";
      }
      var d = assign({}, t);
      if ("Punctuation" === f.kind) {
        if (/^[{([]/.test(f.value)) {
          if (void 0 !== t.indentLevel) {
            t.levels = (t.levels || []).concat(t.indentLevel + 1);
          }
        } else if (/^[})\]]/.test(f.value)) {
          var v = t.levels = (t.levels || []).slice(0, -1);
          if (t.indentLevel && v.length > 0 && v.at(-1) < t.indentLevel) {
            t.indentLevel = v.at(-1);
          }
        }
      }
      while (t.rule) {
        var g = "function" == typeof t.rule ? 0 === t.step ? t.rule(f, e) : null : t.rule[t.step];
        if (t.needsSeparator) {
          g = null == g ? void 0 : g.separator;
        }
        if (g) {
          if (g.ofRule) {
            g = g.ofRule;
          }
          if ("string" == typeof g) {
            pushRule(s, t, g);
            continue;
          }
          if (null === (n = g.match) || void 0 === n ? void 0 : n.call(g, f)) {
            if (g.update) {
              g.update(t, f);
            }
            if ("Punctuation" === f.kind) {
              advanceRule(t, !0);
            } else {
              t.needsAdvance = !0;
            }
            return g.style;
          }
        }
        unsuccessful(t);
      }
      assign(t, d);
      pushRule(S, t, "Invalid");
      return "invalidchar";
    }(e, i, t)
  };
}

function assign(e, t) {
  var i = Object.keys(t);
  for (var n = 0; n < i.length; n++) {
    e[i[n]] = t[i[n]];
  }
  return e;
}

var S = {
  Invalid: [],
  Comment: []
};

function pushRule(e, t, i) {
  if (!e[i]) {
    throw new TypeError("Unknown rule: " + i);
  }
  t.prevState = Object.assign({}, t);
  t.kind = i;
  t.name = null;
  t.type = null;
  t.rule = e[i];
  t.step = 0;
  t.needsSeparator = !1;
}

function popRule(e) {
  if (!e.prevState) {
    return;
  }
  e.kind = e.prevState.kind;
  e.name = e.prevState.name;
  e.type = e.prevState.type;
  e.rule = e.prevState.rule;
  e.step = e.prevState.step;
  e.needsSeparator = e.prevState.needsSeparator;
  e.prevState = e.prevState.prevState;
}

function advanceRule(e, t) {
  var i;
  if (isList(e) && e.rule) {
    var n = e.rule[e.step];
    if (n.separator) {
      var {separator: a} = n;
      e.needsSeparator = !e.needsSeparator;
      if (!e.needsSeparator && a.ofRule) {
        return;
      }
    }
    if (t) {
      return;
    }
  }
  e.needsSeparator = !1;
  e.step++;
  while (e.rule && !(Array.isArray(e.rule) && e.step < e.rule.length)) {
    popRule(e);
    if (e.rule) {
      if (isList(e)) {
        if (null === (i = e.rule) || void 0 === i ? void 0 : i[e.step].separator) {
          e.needsSeparator = !e.needsSeparator;
        }
      } else {
        e.needsSeparator = !1;
        e.step++;
      }
    }
  }
}

function isList(e) {
  var t = Array.isArray(e.rule) && "string" != typeof e.rule[e.step] && e.rule[e.step];
  return t && t.isList;
}

function unsuccessful(e) {
  while (e.rule && (!Array.isArray(e.rule) || !e.rule[e.step].ofRule)) {
    popRule(e);
  }
  if (e.rule) {
    advanceRule(e, !1);
  }
}

class Range {
  constructor(e, t) {
    this.containsPosition = e => {
      if (this.start.line === e.line) {
        return this.start.character <= e.character;
      }
      if (this.end.line === e.line) {
        return this.end.character >= e.character;
      }
      return this.start.line <= e.line && this.end.line >= e.line;
    };
    this.start = e;
    this.end = t;
  }
  setStart(e, t) {
    this.start = new Position(e, t);
  }
  setEnd(e, t) {
    this.end = new Position(e, t);
  }
}

class Position {
  constructor(e, t) {
    this.lessThanOrEqualTo = e => this.line < e.line || this.line === e.line && this.character <= e.character;
    this.line = e;
    this.character = t;
  }
  setLine(e) {
    this.line = e;
  }
  setCharacter(e) {
    this.character = e;
  }
}

var x = "Error", E = "Warning";

var b = {
  [x]: 1,
  [E]: 2
};

var invariant = (e, t) => {
  if (!e) {
    throw new Error(t);
  }
};

function getDiagnostics(f, d = null, v, g, m) {
  var S;
  var x;
  var E = null;
  var A = "";
  if (m) {
    A = "string" == typeof m ? m : m.reduce((e, t) => e + s(t) + "\n\n", "");
  }
  var w = A ? `${f}\n\n${A}` : f;
  try {
    E = l(w);
  } catch (e) {
    if (e instanceof c) {
      var C = function getRange(e, t) {
        var i = onlineParser();
        var n = i.startState();
        var a = t.split("\n");
        invariant(a.length >= e.line, "Query text must have more lines than where the error happened");
        var s = null;
        for (var l = 0; l < e.line; l++) {
          s = new CharacterStream(a[l]);
          while (!s.eol()) {
            if ("invalidchar" === i.token(s, n)) {
              break;
            }
          }
        }
        invariant(s, "Expected Parser stream to be available.");
        var c = e.line - 1;
        var h = s.getStartOfToken();
        var f = s.getCurrentPosition();
        return new Range(new Position(c, h), new Position(c, f));
      }(null !== (x = null === (S = e.locations) || void 0 === S ? void 0 : S[0]) && void 0 !== x ? x : {
        line: 0
      }, w);
      return [ {
        severity: b.Error,
        message: e.message,
        source: "GraphQL: Syntax",
        range: C
      } ];
    }
    throw e;
  }
  return function validateQuery(s, l = null, c, f) {
    if (!l) {
      return [];
    }
    var d = function validateWithCustomRules(s, l, c, h, f) {
      var d = t.filter(e => {
        if (e === i || e === n) {
          return !1;
        }
        return !0;
      });
      return a(s, l, d).filter(t => {
        if (t.message.includes("Unknown directive") && t.nodes) {
          var i = t.nodes[0];
          if (i && i.kind === e.DIRECTIVE) {
            var n = i.name.value;
            if ("arguments" === n || "argumentDefinitions" === n) {
              return !1;
            }
          }
        }
        return !0;
      });
    }(l, s).flatMap(e => annotations(e, b.Error, "Validation"));
    var v = a(l, s, [ h ]).flatMap(e => annotations(e, b.Warning, "Deprecation"));
    return d.concat(v);
  }(E, d);
}

function annotations(e, t, i) {
  if (!e.nodes) {
    return [];
  }
  var n = [];
  for (var [a, s] of e.nodes.entries()) {
    var l = "Variable" !== s.kind && "name" in s && void 0 !== s.name ? s.name : "variable" in s && void 0 !== s.variable ? s.variable : s;
    if (l) {
      invariant(e.locations, "GraphQL validation error requires locations.");
      var c = e.locations[a];
      var h = getLocation(l);
      var f = c.column + (h.end - h.start);
      n.push({
        source: `GraphQL: ${i}`,
        message: e.message,
        severity: t,
        range: new Range(new Position(c.line - 1, c.column - 1), new Position(c.line - 1, f))
      });
    }
  }
  return n;
}

function getLocation(e) {
  var t = e.loc;
  invariant(t, "Expected ASTNode to have a location.");
  return t;
}

var A = "object" == typeof performance && performance && "function" == typeof performance.now ? performance : Date;

var w = new Set, C = "object" == typeof process && process ? process : {};

var T = e => !!e && e === Math.floor(e) && e > 0 && isFinite(e), G = e => T(e) ? e <= Math.pow(2, 8) ? Uint8Array : e <= Math.pow(2, 16) ? Uint16Array : e <= Math.pow(2, 32) ? Uint32Array : e <= Number.MAX_SAFE_INTEGER ? D : null : null, D = class extends Array {
  constructor(e) {
    super(e), this.fill(0);
  }
}, F = class u {
  static #e=!1;
  static create(e) {
    var t = G(e);
    if (!t) {
      return [];
    }
    u.#e = !0;
    var i = new u(e, t);
    return u.#e = !1, i;
  }
  constructor(e, t) {
    if (!u.#e) {
      throw new TypeError("instantiate Stack using Stack.create(n)");
    }
    this.heap = new t(e), this.length = 0;
  }
  push(e) {
    this.heap[this.length++] = e;
  }
  pop() {
    return this.heap[--this.length];
  }
}, k = class u {
  #e;
  #t;
  #i;
  #r;
  #n;
  #a;
  #s;
  #o;
  get perf() {
    return this.#o;
  }
  #l;
  #u;
  #c;
  #h;
  #f;
  #d;
  #p;
  #v;
  #g;
  #m;
  #S;
  #y;
  #x;
  #E;
  #T;
  #b;
  #A;
  #w;
  #C;
  static unsafeExposeInternals(e) {
    return {
      starts: e.#x,
      ttls: e.#E,
      autopurgeTimers: e.#T,
      sizes: e.#y,
      keyMap: e.#c,
      keyList: e.#h,
      valList: e.#f,
      next: e.#d,
      prev: e.#p,
      get head() {
        return e.#v;
      },
      get tail() {
        return e.#g;
      },
      free: e.#m,
      isBackgroundFetch: t => e.#D(t),
      backgroundFetch: (t, i, n, a) => e.#F(t, i, n, a),
      moveToTail: t => e.#k(t),
      indexes: t => e.#N(t),
      rindexes: t => e.#_(t),
      isStale: t => e.#O(t)
    };
  }
  get max() {
    return this.#e;
  }
  get maxSize() {
    return this.#t;
  }
  get calculatedSize() {
    return this.#u;
  }
  get size() {
    return this.#l;
  }
  get fetchMethod() {
    return this.#a;
  }
  get memoMethod() {
    return this.#s;
  }
  get dispose() {
    return this.#i;
  }
  get onInsert() {
    return this.#r;
  }
  get disposeAfter() {
    return this.#n;
  }
  constructor(e) {
    var {max: t = 0, ttl: i, ttlResolution: n = 1, ttlAutopurge: a, updateAgeOnGet: s, updateAgeOnHas: l, allowStale: c, dispose: h, onInsert: f, disposeAfter: d, noDisposeOnSet: v, noUpdateTTL: g, maxSize: m = 0, maxEntrySize: S = 0, sizeCalculation: x, fetchMethod: E, memoMethod: b, noDeleteOnFetchRejection: D, noDeleteOnStaleGet: k, allowStaleOnFetchRejection: N, allowStaleOnFetchAbort: _, ignoreFetchAbort: O, backgroundFetchSize: I = 1, perf: L} = e;
    if (this.backgroundFetchSize = I, void 0 !== L && "function" != typeof L?.now) {
      throw new TypeError("perf option must have a now() method if specified");
    }
    if (this.#o = L ?? A, 0 !== t && !T(t)) {
      throw new TypeError("max option must be a nonnegative integer");
    }
    var P = t ? G(t) : Array;
    if (!P) {
      throw new Error("invalid max value: " + t);
    }
    if (this.#e = t, this.#t = m, this.maxEntrySize = S || this.#t, this.sizeCalculation = x, 
    this.sizeCalculation) {
      if (!this.#t && !this.maxEntrySize) {
        throw new TypeError("cannot set sizeCalculation without setting maxSize or maxEntrySize");
      }
      if ("function" != typeof this.sizeCalculation) {
        throw new TypeError("sizeCalculation set to non-function");
      }
    }
    if (void 0 !== b && "function" != typeof b) {
      throw new TypeError("memoMethod must be a function if defined");
    }
    if (this.#s = b, void 0 !== E && "function" != typeof E) {
      throw new TypeError("fetchMethod must be a function if specified");
    }
    if (this.#a = E, this.#A = !!E, this.#c = new Map, this.#h = Array.from({
      length: t
    }).fill(void 0), this.#f = Array.from({
      length: t
    }).fill(void 0), this.#d = new P(t), this.#p = new P(t), this.#v = 0, this.#g = 0, 
    this.#m = F.create(t), this.#l = 0, this.#u = 0, "function" == typeof h && (this.#i = h), 
    "function" == typeof f && (this.#r = f), "function" == typeof d ? (this.#n = d, 
    this.#S = []) : (this.#n = void 0, this.#S = void 0), this.#b = !!this.#i, this.#C = !!this.#r, 
    this.#w = !!this.#n, this.noDisposeOnSet = !!v, this.noUpdateTTL = !!g, this.noDeleteOnFetchRejection = !!D, 
    this.allowStaleOnFetchRejection = !!N, this.allowStaleOnFetchAbort = !!_, this.ignoreFetchAbort = !!O, 
    0 !== this.maxEntrySize) {
      if (0 !== this.#t && !T(this.#t)) {
        throw new TypeError("maxSize must be a positive integer if specified");
      }
      if (!T(this.maxEntrySize)) {
        throw new TypeError("maxEntrySize must be a positive integer if specified");
      }
      this.#I();
    }
    if (this.allowStale = !!c, this.noDeleteOnStaleGet = !!k, this.updateAgeOnGet = !!s, 
    this.updateAgeOnHas = !!l, this.ttlResolution = T(n) || 0 === n ? n : 1, this.ttlAutopurge = !!a, 
    this.ttl = i || 0, this.ttl) {
      if (!T(this.ttl)) {
        throw new TypeError("ttl must be a positive integer if specified");
      }
      this.#L();
    }
    if (0 === this.#e && 0 === this.ttl && 0 === this.#t) {
      throw new TypeError("At least one of max, maxSize, or ttl is required");
    }
    if (!this.ttlAutopurge && !this.#e && !this.#t) {
      var V = "LRU_CACHE_UNBOUNDED";
      (e => !w.has(e))(V) && (w.add(V), ((e, t, i, n) => {
        "function" == typeof C.emitWarning ? C.emitWarning(e, t, i, n) : console.error(`[${i}] ${t}: ${e}`);
      })("TTL caching without ttlAutopurge, max, or maxSize can result in unbounded memory consumption.", "UnboundedCacheWarning", V, u));
    }
  }
  getRemainingTTL(e) {
    return this.#c.has(e) ? 1 / 0 : 0;
  }
  #L() {
    var e = new D(this.#e), t = new D(this.#e);
    this.#E = e, this.#x = t;
    var i = this.ttlAutopurge ? Array.from({
      length: this.#e
    }) : void 0;
    this.#T = i, this.#P = (i, a, s = this.#o.now()) => {
      t[i] = 0 !== a ? s : 0, e[i] = a, n(i, a);
    }, this.#V = i => {
      t[i] = 0 !== e[i] ? this.#o.now() : 0, n(i, e[i]);
    };
    var n = this.ttlAutopurge ? (e, t) => {
      if (i?.[e] && (clearTimeout(i[e]), i[e] = void 0), t && 0 !== t && i) {
        var n = setTimeout(() => {
          this.#O(e) && this.#z(this.#h[e], "expire");
        }, t + 1);
        n.unref && n.unref(), i[e] = n;
      }
    } : () => {};
    this.#R = (i, n) => {
      if (e[n]) {
        var s = e[n], l = t[n];
        if (!s || !l) {
          return;
        }
        i.ttl = s, i.start = l, i.now = a || r();
        i.remainingTTL = s - (i.now - l);
      }
    };
    var a = 0, r = () => {
      var e = this.#o.now();
      if (this.ttlResolution > 0) {
        a = e;
        var t = setTimeout(() => a = 0, this.ttlResolution);
        t.unref && t.unref();
      }
      return e;
    };
    this.getRemainingTTL = i => {
      var n = this.#c.get(i);
      if (void 0 === n) {
        return 0;
      }
      var s = e[n], l = t[n];
      if (!s || !l) {
        return 1 / 0;
      }
      return s - ((a || r()) - l);
    }, this.#O = i => {
      var n = t[i], s = e[i];
      return !!s && !!n && (a || r()) - n > s;
    };
  }
  #V=() => {};
  #R=() => {};
  #P=() => {};
  #O=() => !1;
  #I() {
    var e = new D(this.#e);
    this.#u = 0, this.#y = e, this.#$ = t => {
      this.#u -= e[t], e[t] = 0;
    }, this.#W = (e, t, i, n) => {
      if (!T(i)) {
        if (this.#D(t)) {
          return this.backgroundFetchSize;
        }
        if (n) {
          if ("function" != typeof n) {
            throw new TypeError("sizeCalculation must be a function");
          }
          if (i = n(t, e), !T(i)) {
            throw new TypeError("sizeCalculation return invalid (expect positive integer)");
          }
        } else {
          throw new TypeError("invalid size value (must be positive integer). When maxSize or maxEntrySize is used, sizeCalculation or size must be set.");
        }
      }
      return i;
    }, this.#j = (t, i, n) => {
      if (e[t] = i, this.#t) {
        var a = this.#t - e[t];
        for (;this.#u > a; ) {
          this.#B(!0);
        }
      }
      this.#u += e[t], n && (n.entrySize = i, n.totalCalculatedSize = this.#u);
    };
  }
  #$=e => {};
  #j=(e, t, i) => {};
  #W=(e, t, i, n) => {
    if (i || n) {
      throw new TypeError("cannot set size without setting maxSize or maxEntrySize on cache");
    }
    return 0;
  };
  * #N({allowStale: e = this.allowStale} = {}) {
    if (this.#l) {
      for (var t = this.#g; this.#M(t) && ((e || !this.#O(t)) && (yield t), t !== this.#v); ) {
        t = this.#p[t];
      }
    }
  }
  * #_({allowStale: e = this.allowStale} = {}) {
    if (this.#l) {
      for (var t = this.#v; this.#M(t) && ((e || !this.#O(t)) && (yield t), t !== this.#g); ) {
        t = this.#d[t];
      }
    }
  }
  #M(e) {
    return void 0 !== e && this.#c.get(this.#h[e]) === e;
  }
  * entries() {
    for (var e of this.#N()) {
      void 0 !== this.#f[e] && void 0 !== this.#h[e] && !this.#D(this.#f[e]) && (yield [ this.#h[e], this.#f[e] ]);
    }
  }
  * rentries() {
    for (var e of this.#_()) {
      void 0 !== this.#f[e] && void 0 !== this.#h[e] && !this.#D(this.#f[e]) && (yield [ this.#h[e], this.#f[e] ]);
    }
  }
  * keys() {
    for (var e of this.#N()) {
      var t = this.#h[e];
      void 0 !== t && !this.#D(this.#f[e]) && (yield t);
    }
  }
  * rkeys() {
    for (var e of this.#_()) {
      var t = this.#h[e];
      void 0 !== t && !this.#D(this.#f[e]) && (yield t);
    }
  }
  * values() {
    for (var e of this.#N()) {
      void 0 !== this.#f[e] && !this.#D(this.#f[e]) && (yield this.#f[e]);
    }
  }
  * rvalues() {
    for (var e of this.#_()) {
      void 0 !== this.#f[e] && !this.#D(this.#f[e]) && (yield this.#f[e]);
    }
  }
  [Symbol.iterator]() {
    return this.entries();
  }
  [Symbol.toStringTag]="LRUCache";
  find(e, t = {}) {
    for (var i of this.#N()) {
      var n = this.#f[i], a = this.#D(n) ? n.__staleWhileFetching : n;
      if (void 0 !== a && e(a, this.#h[i], this)) {
        return this.#K(this.#h[i], t);
      }
    }
  }
  forEach(e, t = this) {
    for (var i of this.#N()) {
      var n = this.#f[i], a = this.#D(n) ? n.__staleWhileFetching : n;
      void 0 !== a && e.call(t, a, this.#h[i], this);
    }
  }
  rforEach(e, t = this) {
    for (var i of this.#_()) {
      var n = this.#f[i], a = this.#D(n) ? n.__staleWhileFetching : n;
      void 0 !== a && e.call(t, a, this.#h[i], this);
    }
  }
  purgeStale() {
    var e = !1;
    for (var t of this.#_({
      allowStale: !0
    })) {
      this.#O(t) && (this.#z(this.#h[t], "expire"), e = !0);
    }
    return e;
  }
  info(e) {
    var t = this.#c.get(e);
    if (void 0 === t) {
      return;
    }
    var i = this.#f[t], n = this.#D(i) ? i.__staleWhileFetching : i;
    if (void 0 === n) {
      return;
    }
    var a = {
      value: n
    };
    if (this.#E && this.#x) {
      var s = this.#E[t], l = this.#x[t];
      if (s && l) {
        var c = s - (this.#o.now() - l);
        a.ttl = c, a.start = Date.now();
      }
    }
    return this.#y && (a.size = this.#y[t]), a;
  }
  dump() {
    var e = [];
    for (var t of this.#N({
      allowStale: !0
    })) {
      var i = this.#h[t], n = this.#f[t], a = this.#D(n) ? n.__staleWhileFetching : n;
      if (void 0 === a || void 0 === i) {
        continue;
      }
      var s = {
        value: a
      };
      if (this.#E && this.#x) {
        s.ttl = this.#E[t];
        var l = this.#o.now() - this.#x[t];
        s.start = Math.floor(Date.now() - l);
      }
      this.#y && (s.size = this.#y[t]), e.unshift([ i, s ]);
    }
    return e;
  }
  load(e) {
    this.clear();
    for (var [t, i] of e) {
      if (i.start) {
        var n = Date.now() - i.start;
        i.start = this.#o.now() - n;
      }
      this.#U(t, i.value, i);
    }
  }
  set(e, t, i = {}) {
    var {status: n} = i;
    i.status = n, n && (n.op = "set", n.key = e, void 0 !== t && (n.value = t), n.cache = this);
    return this.#U(e, t, i);
  }
  #U(e, t, i, n) {
    var {ttl: a = this.ttl, start: s, noDisposeOnSet: l = this.noDisposeOnSet, sizeCalculation: c = this.sizeCalculation, status: h} = i, f = this.#D(t);
    if (void 0 === t) {
      return h && (h.set = "deleted"), this.delete(e), this;
    }
    var {noUpdateTTL: d = this.noUpdateTTL} = i;
    h && !f && (h.value = t);
    var v = this.#W(e, t, i.size || 0, c, h);
    if (this.maxEntrySize && v > this.maxEntrySize) {
      return this.#z(e, "set"), h && (h.set = "miss", h.maxEntrySizeExceeded = !0), this;
    }
    var g = 0 === this.#l ? void 0 : this.#c.get(e);
    if (void 0 === g) {
      g = 0 === this.#l ? this.#g : 0 !== this.#m.length ? this.#m.pop() : this.#l === this.#e ? this.#B(!1) : this.#l, 
      this.#h[g] = e, this.#f[g] = t, this.#c.set(e, g), this.#d[this.#g] = g, this.#p[g] = this.#g, 
      this.#g = g, this.#l++, this.#j(g, v, h), h && (h.set = "add"), d = !1, this.#C && !f && this.#r?.(t, e, "add");
    } else {
      this.#k(g);
      var m = this.#f[g];
      if (t !== m) {
        if (!l) {
          if (this.#D(m)) {
            m !== n && m.__abortController.abort(new Error("replaced"));
            var {__staleWhileFetching: S} = m;
            void 0 !== S && S !== t && (this.#b && this.#i?.(S, e, "set"), this.#w && this.#S?.push([ S, e, "set" ]));
          } else {
            this.#b && this.#i?.(m, e, "set"), this.#w && this.#S?.push([ m, e, "set" ]);
          }
        }
        if (this.#$(g), this.#j(g, v, h), this.#f[g] = t, !f) {
          var x = m && this.#D(m) ? m.__staleWhileFetching : m, E = void 0 === x ? "add" : t !== x ? "replace" : "update";
          h && (h.set = E, void 0 !== x && (h.oldValue = x)), this.#C && this.onInsert?.(t, e, E);
        }
      } else {
        f || (h && (h.set = "update"), this.#C && this.onInsert?.(t, e, "update"));
      }
    }
    if (0 !== a && !this.#E && this.#L(), this.#E && (d || this.#P(g, a, s), h && this.#R(h, g)), 
    !l && this.#w && this.#S) {
      var b, A = this.#S;
      for (;b = A?.shift(); ) {
        this.#n?.(...b);
      }
    }
    return this;
  }
  pop() {
    try {
      for (;this.#l; ) {
        var e = this.#f[this.#v];
        if (this.#B(!0), this.#D(e)) {
          if (e.__staleWhileFetching) {
            return e.__staleWhileFetching;
          }
        } else if (void 0 !== e) {
          return e;
        }
      }
    } finally {
      if (this.#w && this.#S) {
        var t, i = this.#S;
        for (;t = i?.shift(); ) {
          this.#n?.(...t);
        }
      }
    }
  }
  #B(e) {
    var t = this.#v, i = this.#h[t], n = this.#f[t], a = this.#D(n);
    a && n.__abortController.abort(new Error("evicted"));
    var s = a ? n.__staleWhileFetching : n;
    return (this.#b || this.#w) && void 0 !== s && (this.#b && this.#i?.(s, i, "evict"), 
    this.#w && this.#S?.push([ s, i, "evict" ])), this.#$(t), this.#T?.[t] && (clearTimeout(this.#T[t]), 
    this.#T[t] = void 0), e && (this.#h[t] = void 0, this.#f[t] = void 0, this.#m.push(t)), 
    1 === this.#l ? (this.#v = this.#g = 0, this.#m.length = 0) : this.#v = this.#d[t], 
    this.#c.delete(i), this.#l--, t;
  }
  has(e, t = {}) {
    var {status: i} = t;
    t.status = i, i && (i.op = "has", i.key = e, i.cache = this);
    return this.#G(e, t);
  }
  #G(e, t = {}) {
    var {updateAgeOnHas: i = this.updateAgeOnHas, status: n} = t, a = this.#c.get(e);
    if (void 0 !== a) {
      var s = this.#f[a];
      if (this.#D(s) && void 0 === s.__staleWhileFetching) {
        return !1;
      }
      if (this.#O(a)) {
        n && (n.has = "stale", this.#R(n, a));
      } else {
        return i && this.#V(a), n && (n.has = "hit", this.#R(n, a)), !0;
      }
    } else {
      n && (n.has = "miss");
    }
    return !1;
  }
  peek(e, t = {}) {
    var {status: i} = t;
    i && (i.op = "peek", i.key = e, i.cache = this), t.status = i;
    return this.#Q(e, t);
  }
  #Q(e, t) {
    var {status: i, allowStale: n = this.allowStale} = t, a = this.#c.get(e);
    if (void 0 === a || !n && this.#O(a)) {
      i && (i.peek = void 0 === a ? "miss" : "stale");
      return;
    }
    var s = this.#f[a], l = this.#D(s) ? s.__staleWhileFetching : s;
    return i && (void 0 !== l ? (i.peek = "hit", i.value = l) : i.peek = "miss"), l;
  }
  #F(e, t, i, n) {
    var a = void 0 === t ? void 0 : this.#f[t];
    if (this.#D(a)) {
      return a;
    }
    var s = new AbortController, {signal: l} = i;
    l?.addEventListener("abort", () => s.abort(l.reason), {
      signal: s.signal
    });
    var c = {
      signal: s.signal,
      options: i,
      context: n
    }, o = (n, a = !1) => {
      var {aborted: l} = s.signal, f = i.ignoreFetchAbort && void 0 !== n, d = i.ignoreFetchAbort || !(!i.allowStaleOnFetchAbort || void 0 === n);
      if (i.status && (l && !a ? (i.status.fetchAborted = !0, i.status.fetchError = s.signal.reason, 
      f && (i.status.fetchAbortIgnored = !0)) : i.status.fetchResolved = !0), l && !f && !a) {
        return y(s.signal.reason, d);
      }
      var v = h, g = this.#f[t];
      return (g === h || void 0 === g && f && a) && (void 0 === n ? void 0 !== v.__staleWhileFetching ? this.#f[t] = v.__staleWhileFetching : this.#z(e, "fetch") : (i.status && (i.status.fetchUpdated = !0), 
      this.#U(e, n, c.options, v))), n;
    }, y = (n, a) => {
      var {aborted: l} = s.signal, c = l && i.allowStaleOnFetchAbort, f = c || i.allowStaleOnFetchRejection, d = h;
      if (this.#f[t] === h && (!(f || i.noDeleteOnFetchRejection) || !a && void 0 === d.__staleWhileFetching ? this.#z(e, "fetch") : c || (this.#f[t] = d.__staleWhileFetching)), 
      f) {
        return i.status && void 0 !== d.__staleWhileFetching && (i.status.returnedStale = !0), 
        d.__staleWhileFetching;
      }
      if (d.__returned === d) {
        throw n;
      }
    };
    i.status && (i.status.fetchDispatched = !0);
    var h = new Promise((t, n) => {
      var l = this.#a?.(e, a, c);
      s.signal.addEventListener("abort", () => {
        (!i.ignoreFetchAbort || i.allowStaleOnFetchAbort) && (t(void 0), i.allowStaleOnFetchAbort && (t = e => o(e, !0)));
      }), l && l instanceof Promise ? l.then(e => t(void 0 === e ? void 0 : e), n) : void 0 !== l && t(l);
    }).then(o, e => (i.status && (i.status.fetchRejected = !0, i.status.fetchError = e), 
    y(e, !1))), f = Object.assign(h, {
      __abortController: s,
      __staleWhileFetching: a,
      __returned: void 0
    });
    return void 0 === t ? (this.#U(e, f, {
      ...c.options,
      status: void 0
    }), t = this.#c.get(e)) : this.#f[t] = f, f;
  }
  #D(e) {
    if (!this.#A) {
      return !1;
    }
    var t = e;
    return !!t && t instanceof Promise && t.hasOwnProperty("__staleWhileFetching") && t.__abortController instanceof AbortController;
  }
  fetch(e, t = {}) {
    var {status: i} = t;
    t.status = i, i && t.context && (i.context = t.context);
    return this.#q(e, t);
  }
  async #q(e, t = {}) {
    var {allowStale: i = this.allowStale, updateAgeOnGet: n = this.updateAgeOnGet, noDeleteOnStaleGet: a = this.noDeleteOnStaleGet, ttl: s = this.ttl, noDisposeOnSet: l = this.noDisposeOnSet, size: c = 0, sizeCalculation: h = this.sizeCalculation, noUpdateTTL: f = this.noUpdateTTL, noDeleteOnFetchRejection: d = this.noDeleteOnFetchRejection, allowStaleOnFetchRejection: v = this.allowStaleOnFetchRejection, ignoreFetchAbort: g = this.ignoreFetchAbort, allowStaleOnFetchAbort: m = this.allowStaleOnFetchAbort, context: S, forceRefresh: x = !1, status: E, signal: b} = t;
    if (E && (E.op = "fetch", E.key = e, x && (E.forceRefresh = !0), E.cache = this), 
    !this.#A) {
      return E && (E.fetch = "get"), this.#K(e, {
        allowStale: i,
        updateAgeOnGet: n,
        noDeleteOnStaleGet: a,
        status: E
      });
    }
    var A = {
      allowStale: i,
      updateAgeOnGet: n,
      noDeleteOnStaleGet: a,
      ttl: s,
      noDisposeOnSet: l,
      size: c,
      sizeCalculation: h,
      noUpdateTTL: f,
      noDeleteOnFetchRejection: d,
      allowStaleOnFetchRejection: v,
      allowStaleOnFetchAbort: m,
      ignoreFetchAbort: g,
      status: E,
      signal: b
    }, w = this.#c.get(e);
    if (void 0 === w) {
      E && (E.fetch = "miss");
      var C = this.#F(e, w, A, S);
      return C.__returned = C;
    } else {
      var D = this.#f[w];
      if (this.#D(D)) {
        var F = i && void 0 !== D.__staleWhileFetching;
        return E && (E.fetch = "inflight", F && (E.returnedStale = !0)), F ? D.__staleWhileFetching : D.__returned = D;
      }
      var k = this.#O(w);
      if (!x && !k) {
        return E && (E.fetch = "hit"), this.#k(w), n && this.#V(w), E && this.#R(E, w), 
        D;
      }
      var N = this.#F(e, w, A, S), _ = void 0 !== N.__staleWhileFetching && i;
      return E && (E.fetch = k ? "stale" : "refresh", _ && k && (E.returnedStale = !0)), 
      _ ? N.__staleWhileFetching : N.__returned = N;
    }
  }
  forceFetch(e, t = {}) {
    var {status: i} = t;
    t.status = i, i && t.context && (i.context = t.context);
    return this.#J(e, t);
  }
  async #J(e, t = {}) {
    var i = await this.#q(e, t);
    if (void 0 === i) {
      throw new Error("fetch() returned undefined");
    }
    return i;
  }
  memo(e, t = {}) {
    var {status: i} = t;
    t.status = i, i && (i.op = "memo", i.key = e, t.context && (i.context = t.context), 
    i.cache = this);
    var n = this.#X(e, t);
    return i && (i.value = n), n;
  }
  #X(e, t = {}) {
    var i = this.#s;
    if (!i) {
      throw new Error("no memoMethod provided to constructor");
    }
    var {context: n, status: a, forceRefresh: s, ...l} = t;
    a && s && (a.forceRefresh = !0);
    var c = this.#K(e, l), h = s || void 0 === c;
    if (a && (a.memo = h ? "miss" : "hit", h || (a.value = c)), !h) {
      return c;
    }
    var f = i(e, c, {
      options: l,
      context: n
    });
    return a && (a.value = f), this.#U(e, f, l), f;
  }
  get(e, t = {}) {
    var {status: i} = t;
    t.status = i, i && (i.op = "get", i.key = e, i.cache = this);
    var n = this.#K(e, t);
    return i && (void 0 !== n && (i.value = n)), n;
  }
  #K(e, t = {}) {
    var {allowStale: i = this.allowStale, updateAgeOnGet: n = this.updateAgeOnGet, noDeleteOnStaleGet: a = this.noDeleteOnStaleGet, status: s} = t, l = this.#c.get(e);
    if (void 0 === l) {
      s && (s.get = "miss");
      return;
    }
    var c = this.#f[l], h = this.#D(c);
    return s && this.#R(s, l), this.#O(l) ? h ? (s && (s.get = "stale-fetching"), i && void 0 !== c.__staleWhileFetching ? (s && (s.returnedStale = !0), 
    c.__staleWhileFetching) : void 0) : (a || this.#z(e, "expire"), s && (s.get = "stale"), 
    i ? (s && (s.returnedStale = !0), c) : void 0) : (s && (s.get = h ? "fetching" : "hit"), 
    this.#k(l), n && this.#V(l), h ? c.__staleWhileFetching : c);
  }
  #Y(e, t) {
    this.#p[t] = e, this.#d[e] = t;
  }
  #k(e) {
    e !== this.#g && (e === this.#v ? this.#v = this.#d[e] : this.#Y(this.#p[e], this.#d[e]), 
    this.#Y(this.#g, e), this.#g = e);
  }
  delete(e) {
    return this.#z(e, "delete");
  }
  #z(e, t) {
    var i = !1;
    if (0 !== this.#l) {
      var n = this.#c.get(e);
      if (void 0 !== n) {
        if (this.#T?.[n] && (clearTimeout(this.#T?.[n]), this.#T[n] = void 0), i = !0, 1 === this.#l) {
          this.#H(t);
        } else {
          this.#$(n);
          var a = this.#f[n];
          if (this.#D(a) ? a.__abortController.abort(new Error("deleted")) : (this.#b || this.#w) && (this.#b && this.#i?.(a, e, t), 
          this.#w && this.#S?.push([ a, e, t ])), this.#c.delete(e), this.#h[n] = void 0, 
          this.#f[n] = void 0, n === this.#g) {
            this.#g = this.#p[n];
          } else if (n === this.#v) {
            this.#v = this.#d[n];
          } else {
            var s = this.#p[n];
            this.#d[s] = this.#d[n];
            var l = this.#d[n];
            this.#p[l] = this.#p[n];
          }
          this.#l--, this.#m.push(n);
        }
      }
    }
    if (this.#w && this.#S?.length) {
      var c, h = this.#S;
      for (;c = h?.shift(); ) {
        this.#n?.(...c);
      }
    }
    return i;
  }
  clear() {
    return this.#H("delete");
  }
  #H(e) {
    for (var t of this.#_({
      allowStale: !0
    })) {
      var i = this.#f[t];
      if (this.#D(i)) {
        i.__abortController.abort(new Error("deleted"));
      } else {
        var n = this.#h[t];
        this.#b && this.#i?.(i, n, e), this.#w && this.#S?.push([ i, n, e ]);
      }
    }
    if (this.#c.clear(), this.#f.fill(void 0), this.#h.fill(void 0), this.#E && this.#x) {
      this.#E.fill(0), this.#x.fill(0);
      for (var a of this.#T ?? []) {
        void 0 !== a && clearTimeout(a);
      }
      this.#T?.fill(void 0);
    }
    if (this.#y && this.#y.fill(0), this.#v = 0, this.#g = 0, this.#m.length = 0, this.#u = 0, 
    this.#l = 0, this.#w && this.#S) {
      var s, l = this.#S;
      for (;s = l?.shift(); ) {
        this.#n?.(...s);
      }
    }
  }
};

var N = {
  32: 16777619n,
  64: 1099511628211n,
  128: 309485009821345068724781371n,
  256: 374144419156711147060143317175368453031918731002211n,
  512: 35835915874844867368919076489095108449946327955754392558399825615420669938882575126094039892345713852759n,
  1024: 5016456510113118655434598811035278955030765345404790744303017523831112055108147451509157692220295382716162651878526895249385292291816524375083746691371804094271873160484737966720260389217684476157468082573n
};

var _ = {
  32: 2166136261n,
  64: 14695981039346656037n,
  128: 144066263297769815596495629667062367629n,
  256: 100029257958052580907070968620625704837092796014241193945225284501741471925557n,
  512: 9659303129496669498009435400716310466090418745672637896108374329434462657994582932197716438449813051892206539805784495328239340083876191928701583869517785n,
  1024: 
};

var O = new globalThis.TextEncoder;

function fnv1a(e, {size: t = 32, utf8Buffer: i} = {}) {
  if (!N[t]) {
    throw new Error("The `size` option must be one of 32, 64, 128, 256, 512, or 1024");
  }
  if ("string" == typeof e) {
    if (i) {
      return function fnv1aEncodeInto(e, t, i) {
        if (0 === i.length) {
          throw new Error("The `utf8Buffer` option must have a length greater than zero");
        }
        var n = N[t];
        var a = _[t];
        var s = e;
        while (s.length > 0) {
          var l = O.encodeInto(s, i);
          s = s.slice(l.read);
          for (var c = 0; c < l.written; c++) {
            a ^= BigInt(i[c]);
            a = BigInt.asUintN(t, a * n);
          }
        }
        return a;
      }(e, t, i);
    }
    e = O.encode(e);
  }
  return function fnv1aUint8Array(e, t) {
    var i = N[t];
    var n = _[t];
    for (var a = 0; a < e.length; a++) {
      n ^= BigInt(e[a]);
      n = BigInt.asUintN(t, n * i);
    }
    return n;
  }(e, t);
}

var I = "FragmentDefinition";

class GraphQLError extends Error {
  constructor(e, t, i, n, a, s, l) {
    if (super(e), this.name = "GraphQLError", this.message = e, a) {
      this.path = a;
    }
    if (t) {
      this.nodes = Array.isArray(t) ? t : [ t ];
    }
    if (i) {
      this.source = i;
    }
    if (n) {
      this.positions = n;
    }
    if (s) {
      this.originalError = s;
    }
    var c = l;
    if (!c && s) {
      var h = s.extensions;
      if (h && "object" == typeof h) {
        c = h;
      }
    }
    this.extensions = c || {};
  }
  toJSON() {
    return {
      ...this,
      message: this.message
    };
  }
  toString() {
    return this.message;
  }
  get [Symbol.toStringTag]() {
    return "GraphQLError";
  }
}

var L;

var P;

function error(e) {
  return new GraphQLError(`Syntax Error: Unexpected token at ${P} in ${e}`);
}

function advance(e) {
  if (e.lastIndex = P, e.test(L)) {
    return L.slice(P, P = e.lastIndex);
  }
}

var V = / +(?=[^\s])/y;

function blockString(e) {
  var t = e.split("\n");
  var i = "";
  var n = 0;
  var a = 0;
  var s = t.length - 1;
  for (var l = 0; l < t.length; l++) {
    if (V.lastIndex = 0, V.test(t[l])) {
      if (l && (!n || V.lastIndex < n)) {
        n = V.lastIndex;
      }
      a = a || l, s = l;
    }
  }
  for (var c = a; c <= s; c++) {
    if (c !== a) {
      i += "\n";
    }
    i += t[c].slice(n).replace(/\\"""/g, '"""');
  }
  return i;
}

function ignored() {
  for (var e = 0 | L.charCodeAt(P++); 9 === e || 10 === e || 13 === e || 32 === e || 35 === e || 44 === e || 65279 === e; e = 0 | L.charCodeAt(P++)) {
    if (35 === e) {
      for (;(e = 0 | L.charCodeAt(P++)) && 10 !== e && 13 !== e; ) {}
    }
  }
  P--;
}

function name() {
  var e = P;
  for (var t = 0 | L.charCodeAt(P++); t >= 48 && t <= 57 || t >= 65 && t <= 90 || 95 === t || t >= 97 && t <= 122; t = 0 | L.charCodeAt(P++)) {}
  if (e === P - 1) {
    throw error("Name");
  }
  var i = L.slice(e, --P);
  return ignored(), i;
}

function nameNode() {
  return {
    kind: "Name",
    value: name()
  };
}

var z = /(?:"""|(?:[\s\S]*?[^\\])""")/y;

var R = /(?:(?:\.\d+)?[eE][+-]?\d+|\.\d+)/y;

function value(e) {
  var t;
  switch (L.charCodeAt(P)) {
   case 91:
    P++, ignored();
    var i = [];
    for (;93 !== L.charCodeAt(P); ) {
      i.push(value(e));
    }
    return P++, ignored(), {
      kind: "ListValue",
      values: i
    };

   case 123:
    P++, ignored();
    var n = [];
    for (;125 !== L.charCodeAt(P); ) {
      var a = nameNode();
      if (58 !== L.charCodeAt(P++)) {
        throw error("ObjectField");
      }
      ignored(), n.push({
        kind: "ObjectField",
        name: a,
        value: value(e)
      });
    }
    return P++, ignored(), {
      kind: "ObjectValue",
      fields: n
    };

   case 36:
    if (e) {
      throw error("Variable");
    }
    return P++, {
      kind: "Variable",
      name: nameNode()
    };

   case 34:
    if (34 === L.charCodeAt(P + 1) && 34 === L.charCodeAt(P + 2)) {
      if (P += 3, null == (t = advance(z))) {
        throw error("StringValue");
      }
      return ignored(), {
        kind: "StringValue",
        value: blockString(t.slice(0, -3)),
        block: !0
      };
    } else {
      var s = P;
      var l;
      P++;
      var c = !1;
      for (l = 0 | L.charCodeAt(P++); 92 === l && (P++, c = !0) || 10 !== l && 13 !== l && 34 !== l && l; l = 0 | L.charCodeAt(P++)) {}
      if (34 !== l) {
        throw error("StringValue");
      }
      return t = L.slice(s, P), ignored(), {
        kind: "StringValue",
        value: c ? JSON.parse(t) : t.slice(1, -1),
        block: !1
      };
    }

   case 45:
   case 48:
   case 49:
   case 50:
   case 51:
   case 52:
   case 53:
   case 54:
   case 55:
   case 56:
   case 57:
    var h = P++;
    var f;
    for (;(f = 0 | L.charCodeAt(P++)) >= 48 && f <= 57; ) {}
    var d = L.slice(h, --P);
    if (46 === (f = L.charCodeAt(P)) || 69 === f || 101 === f) {
      if (null == (t = advance(R))) {
        throw error("FloatValue");
      }
      return ignored(), {
        kind: "FloatValue",
        value: d + t
      };
    } else {
      return ignored(), {
        kind: "IntValue",
        value: d
      };
    }

   case 110:
    if (117 === L.charCodeAt(P + 1) && 108 === L.charCodeAt(P + 2) && 108 === L.charCodeAt(P + 3)) {
      return P += 4, ignored(), {
        kind: "NullValue"
      };
    } else {
      break;
    }

   case 116:
    if (114 === L.charCodeAt(P + 1) && 117 === L.charCodeAt(P + 2) && 101 === L.charCodeAt(P + 3)) {
      return P += 4, ignored(), {
        kind: "BooleanValue",
        value: !0
      };
    } else {
      break;
    }

   case 102:
    if (97 === L.charCodeAt(P + 1) && 108 === L.charCodeAt(P + 2) && 115 === L.charCodeAt(P + 3) && 101 === L.charCodeAt(P + 4)) {
      return P += 5, ignored(), {
        kind: "BooleanValue",
        value: !1
      };
    } else {
      break;
    }
  }
  return {
    kind: "EnumValue",
    value: name()
  };
}

function arguments_(e) {
  if (40 === L.charCodeAt(P)) {
    var t = [];
    P++, ignored();
    do {
      var i = nameNode();
      if (58 !== L.charCodeAt(P++)) {
        throw error("Argument");
      }
      ignored(), t.push({
        kind: "Argument",
        name: i,
        value: value(e)
      });
    } while (41 !== L.charCodeAt(P));
    return P++, ignored(), t;
  }
}

function directives(e) {
  if (64 === L.charCodeAt(P)) {
    var t = [];
    do {
      P++, t.push({
        kind: "Directive",
        name: nameNode(),
        arguments: arguments_(e)
      });
    } while (64 === L.charCodeAt(P));
    return t;
  }
}

function type() {
  var e = 0;
  for (;91 === L.charCodeAt(P); ) {
    e++, P++, ignored();
  }
  var t = {
    kind: "NamedType",
    name: nameNode()
  };
  do {
    if (33 === L.charCodeAt(P)) {
      P++, ignored(), t = {
        kind: "NonNullType",
        type: t
      };
    }
    if (e) {
      if (93 !== L.charCodeAt(P++)) {
        throw error("NamedType");
      }
      ignored(), t = {
        kind: "ListType",
        type: t
      };
    }
  } while (e--);
  return t;
}

function selectionSetStart() {
  if (123 !== L.charCodeAt(P++)) {
    throw error("SelectionSet");
  }
  return ignored(), selectionSet();
}

function selectionSet() {
  var e = [];
  do {
    if (46 === L.charCodeAt(P)) {
      if (46 !== L.charCodeAt(++P) || 46 !== L.charCodeAt(++P)) {
        throw error("SelectionSet");
      }
      switch (P++, ignored(), L.charCodeAt(P)) {
       case 64:
        e.push({
          kind: "InlineFragment",
          typeCondition: void 0,
          directives: directives(!1),
          selectionSet: selectionSetStart()
        });
        break;

       case 111:
        if (110 === L.charCodeAt(P + 1)) {
          P += 2, ignored(), e.push({
            kind: "InlineFragment",
            typeCondition: {
              kind: "NamedType",
              name: nameNode()
            },
            directives: directives(!1),
            selectionSet: selectionSetStart()
          });
        } else {
          e.push({
            kind: "FragmentSpread",
            name: nameNode(),
            directives: directives(!1)
          });
        }
        break;

       case 123:
        P++, ignored(), e.push({
          kind: "InlineFragment",
          typeCondition: void 0,
          directives: void 0,
          selectionSet: selectionSet()
        });
        break;

       default:
        e.push({
          kind: "FragmentSpread",
          name: nameNode(),
          directives: directives(!1)
        });
      }
    } else {
      var t = nameNode();
      var i = void 0;
      if (58 === L.charCodeAt(P)) {
        P++, ignored(), i = t, t = nameNode();
      }
      var n = arguments_(!1);
      var a = directives(!1);
      var s = void 0;
      if (123 === L.charCodeAt(P)) {
        P++, ignored(), s = selectionSet();
      }
      e.push({
        kind: "Field",
        alias: i,
        name: t,
        arguments: n,
        directives: a,
        selectionSet: s
      });
    }
  } while (125 !== L.charCodeAt(P));
  return P++, ignored(), {
    kind: "SelectionSet",
    selections: e
  };
}

function variableDefinitions() {
  if (ignored(), 40 === L.charCodeAt(P)) {
    var e = [];
    P++, ignored();
    do {
      var t = void 0;
      if (34 === L.charCodeAt(P)) {
        t = value(!0);
      }
      if (36 !== L.charCodeAt(P++)) {
        throw error("Variable");
      }
      var i = nameNode();
      if (58 !== L.charCodeAt(P++)) {
        throw error("VariableDefinition");
      }
      ignored();
      var n = type();
      var a = void 0;
      if (61 === L.charCodeAt(P)) {
        P++, ignored(), a = value(!0);
      }
      ignored();
      var s = {
        kind: "VariableDefinition",
        variable: {
          kind: "Variable",
          name: i
        },
        type: n,
        defaultValue: a,
        directives: directives(!0)
      };
      if (t) {
        s.description = t;
      }
      e.push(s);
    } while (41 !== L.charCodeAt(P));
    return P++, ignored(), e;
  }
}

function fragmentDefinition(e) {
  var t = nameNode();
  if (111 !== L.charCodeAt(P++) || 110 !== L.charCodeAt(P++)) {
    throw error("FragmentDefinition");
  }
  ignored();
  var i = {
    kind: "FragmentDefinition",
    name: t,
    typeCondition: {
      kind: "NamedType",
      name: nameNode()
    },
    directives: directives(!1),
    selectionSet: selectionSetStart()
  };
  if (e) {
    i.description = e;
  }
  return i;
}

function definitions() {
  var e = [];
  do {
    var t = void 0;
    if (34 === L.charCodeAt(P)) {
      t = value(!0);
    }
    if (123 === L.charCodeAt(P)) {
      if (t) {
        throw error("Document");
      }
      P++, ignored(), e.push({
        kind: "OperationDefinition",
        operation: "query",
        name: void 0,
        variableDefinitions: void 0,
        directives: void 0,
        selectionSet: selectionSet()
      });
    } else {
      var i = name();
      switch (i) {
       case "fragment":
        e.push(fragmentDefinition(t));
        break;

       case "query":
       case "mutation":
       case "subscription":
        var n;
        var a = void 0;
        if (40 !== (n = L.charCodeAt(P)) && 64 !== n && 123 !== n) {
          a = nameNode();
        }
        var s = {
          kind: "OperationDefinition",
          operation: i,
          name: a,
          variableDefinitions: variableDefinitions(),
          directives: directives(!1),
          selectionSet: selectionSetStart()
        };
        if (t) {
          s.description = t;
        }
        e.push(s);
        break;

       default:
        throw error("Document");
      }
    }
  } while (P < L.length);
  return e;
}

function parse(e, t) {
  if (L = e.body ? e.body : e, P = 0, ignored(), t) {} else {
    return {
      kind: "Document",
      definitions: definitions(),
      loc: {
        start: 0,
        end: L.length,
        startToken: void 0,
        endToken: void 0,
        source: {
          body: L,
          name: "graphql.web",
          locationOffset: {
            line: 1,
            column: 1
          }
        }
      }
    };
  }
}

var $ = {};

function visit(e, t) {
  var i = [];
  var n = [];
  try {
    var a = function traverse(e, a, s) {
      var l = !1;
      var c = t[e.kind] && t[e.kind].enter || t[e.kind] || t.enter;
      var h = c && c.call(t, e, a, s, n, i);
      if (!1 === h) {
        return e;
      } else if (null === h) {
        return null;
      } else if (h === $) {
        throw $;
      } else if (h && "string" == typeof h.kind) {
        l = h !== e, e = h;
      }
      if (s) {
        i.push(s);
      }
      var f;
      var d = {
        ...e
      };
      for (var v in e) {
        n.push(v);
        var g = e[v];
        if (Array.isArray(g)) {
          var m = [];
          for (var S = 0; S < g.length; S++) {
            if (null != g[S] && "string" == typeof g[S].kind) {
              if (i.push(e), n.push(S), f = traverse(g[S], S, g), n.pop(), i.pop(), null == f) {
                l = !0;
              } else {
                l = l || f !== g[S], m.push(f);
              }
            }
          }
          g = m;
        } else if (null != g && "string" == typeof g.kind) {
          if (void 0 !== (f = traverse(g, v, e))) {
            l = l || g !== f, g = f;
          }
        }
        if (n.pop(), l) {
          d[v] = g;
        }
      }
      if (s) {
        i.pop();
      }
      var x = t[e.kind] && t[e.kind].leave || t.leave;
      var E = x && x.call(t, e, a, s, n, i);
      if (E === $) {
        throw $;
      } else if (void 0 !== E) {
        return E;
      } else if (void 0 !== h) {
        return l ? d : h;
      } else {
        return l ? d : e;
      }
    }(e);
    return void 0 !== a && !1 !== a ? a : e;
  } catch (t) {
    if (t !== $) {
      throw t;
    }
    return e;
  }
}

function mapJoin(e, t, i) {
  var n = "";
  for (var a = 0; a < e.length; a++) {
    if (a) {
      n += t;
    }
    n += i(e[a]);
  }
  return n;
}

var W = "\n";

var j = {
  OperationDefinition(e) {
    var t = "";
    if (e.description) {
      t += j.StringValue(e.description) + "\n";
    }
    if (t += e.operation, e.name) {
      t += " " + e.name.value;
    }
    if (e.variableDefinitions && e.variableDefinitions.length) {
      if (!e.name) {
        t += " ";
      }
      t += "(" + mapJoin(e.variableDefinitions, ", ", j.VariableDefinition) + ")";
    }
    if (e.directives && e.directives.length) {
      t += " " + mapJoin(e.directives, " ", j.Directive);
    }
    var i = j.SelectionSet(e.selectionSet);
    return "query" !== t ? t + " " + i : i;
  },
  VariableDefinition(e) {
    var t = "";
    if (e.description) {
      t += j.StringValue(e.description) + " ";
    }
    if (t += j.Variable(e.variable) + ": " + _print(e.type), e.defaultValue) {
      t += " = " + _print(e.defaultValue);
    }
    if (e.directives && e.directives.length) {
      t += " " + mapJoin(e.directives, " ", j.Directive);
    }
    return t;
  },
  Field(e) {
    var t = e.alias ? e.alias.value + ": " + e.name.value : e.name.value;
    if (e.arguments && e.arguments.length) {
      var i = mapJoin(e.arguments, ", ", j.Argument);
      if (t.length + i.length + 2 > 80) {
        t += "(" + (W += "  ") + mapJoin(e.arguments, W, j.Argument) + (W = W.slice(0, -2)) + ")";
      } else {
        t += "(" + i + ")";
      }
    }
    if (e.directives && e.directives.length) {
      t += " " + mapJoin(e.directives, " ", j.Directive);
    }
    if (e.selectionSet && e.selectionSet.selections.length) {
      t += " " + j.SelectionSet(e.selectionSet);
    }
    return t;
  },
  StringValue(e) {
    if (e.block) {
      return function printBlockString(e) {
        return '"""\n' + e.replace(/"""/g, '\\"""') + '\n"""';
      }(e.value).replace(/\n/g, W);
    } else {
      return function printString(e) {
        return JSON.stringify(e);
      }(e.value);
    }
  },
  BooleanValue: e => "" + e.value,
  NullValue: e => "null",
  IntValue: e => e.value,
  FloatValue: e => e.value,
  EnumValue: e => e.value,
  Name: e => e.value,
  Variable: e => "$" + e.name.value,
  ListValue: e => "[" + mapJoin(e.values, ", ", _print) + "]",
  ObjectValue: e => "{" + mapJoin(e.fields, ", ", j.ObjectField) + "}",
  ObjectField: e => e.name.value + ": " + _print(e.value),
  Document(e) {
    if (!e.definitions || !e.definitions.length) {
      return "";
    } else {
      return mapJoin(e.definitions, "\n\n", _print);
    }
  },
  SelectionSet: e => "{" + (W += "  ") + mapJoin(e.selections, W, _print) + (W = W.slice(0, -2)) + "}",
  Argument: e => e.name.value + ": " + _print(e.value),
  FragmentSpread(e) {
    var t = "..." + e.name.value;
    if (e.directives && e.directives.length) {
      t += " " + mapJoin(e.directives, " ", j.Directive);
    }
    return t;
  },
  InlineFragment(e) {
    var t = "...";
    if (e.typeCondition) {
      t += " on " + e.typeCondition.name.value;
    }
    if (e.directives && e.directives.length) {
      t += " " + mapJoin(e.directives, " ", j.Directive);
    }
    return t + " " + j.SelectionSet(e.selectionSet);
  },
  FragmentDefinition(e) {
    var t = "";
    if (e.description) {
      t += j.StringValue(e.description) + "\n";
    }
    if (t += "fragment " + e.name.value, t += " on " + e.typeCondition.name.value, e.directives && e.directives.length) {
      t += " " + mapJoin(e.directives, " ", j.Directive);
    }
    return t + " " + j.SelectionSet(e.selectionSet);
  },
  Directive(e) {
    var t = "@" + e.name.value;
    if (e.arguments && e.arguments.length) {
      t += "(" + mapJoin(e.arguments, ", ", j.Argument) + ")";
    }
    return t;
  },
  NamedType: e => e.name.value,
  ListType: e => "[" + _print(e.type) + "]",
  NonNullType: e => _print(e.type) + "!"
};

var _print = e => j[e.kind](e);

function print(e) {
  return W = "\n", j[e.kind] ? j[e.kind](e) : "";
}

var B = new Set([ "gql", "graphql" ]);

var isIIFE = e => v.isCallExpression(e) && 0 === e.arguments.length && (v.isFunctionExpression(e.expression) || v.isArrowFunction(e.expression)) && !e.expression.asteriskToken && !e.expression.modifiers?.length;

var isGraphQLFunctionIdentifier = e => v.isIdentifier(e) && B.has(e.escapedText);

var M = new WeakMap;

var K = new WeakMap;

var getCached = (e, t, i, n) => {
  var a = t.getSymbolAtLocation(i);
  if (!a) {
    return n();
  }
  var s = e.get(t);
  if (!s) {
    e.set(t, s = new WeakMap);
  }
  var l = s.get(a);
  if (void 0 === l) {
    s.set(a, l = n());
  }
  return l;
};

var isTadaGraphQLFunction = (e, t) => {
  if (!v.isLeftHandSideExpression(e) || !t) {
    return !1;
  }
  return getCached(M, t, e, () => {
    var i = t.getTypeAtLocation(e);
    return null != i && null != i.getProperty("scalar") && null != i.getProperty("persisted");
  });
};

var U = /^(?:[\s,]+|#[^\n\r]*)*(?:\{|(?:query|mutation|subscription|fragment)(?=[\s,{(@#]|$))/;

var isTadaGraphQLCall = (e, t) => {
  if (!v.isCallExpression(e)) {
    return !1;
  } else if (e.arguments.length < 1 || e.arguments.length > 2) {
    return !1;
  }
  var i = e.arguments[0];
  var n = v.isTemplateExpression(i) ? i.head.text : v.isStringLiteralLike(i) ? i.text : null;
  if (null == n || !U.test(n)) {
    return !1;
  }
  return t ? isTadaGraphQLFunction(e.expression, t) : !1;
};

var isTadaPersistedCall = (e, t) => {
  if (!e) {
    return !1;
  } else if (!v.isCallExpression(e)) {
    return !1;
  } else if (!v.isPropertyAccessExpression(e.expression)) {
    return !1;
  } else if (!v.isIdentifier(e.expression.name) || "persisted" !== e.expression.name.escapedText) {
    return !1;
  } else if (isGraphQLFunctionIdentifier(e.expression.expression)) {
    return !0;
  } else {
    return isTadaGraphQLFunction(e.expression.expression, t);
  }
};

var isGraphQLCall = (e, t) => v.isCallExpression(e) && e.arguments.length >= 1 && e.arguments.length <= 2 && (isGraphQLFunctionIdentifier(e.expression) || isTadaGraphQLCall(e, t));

var isGraphQLTag = e => v.isTaggedTemplateExpression(e) && isGraphQLFunctionIdentifier(e.tag);

var getSchemaName = (e, t, i = !1) => {
  if (!t) {
    return null;
  }
  var n = i ? e.getChildAt(0).getChildAt(0) : e.expression;
  return getCached(K, t, n, () => {
    var e = t.getTypeAtLocation(n);
    if (e) {
      var i = e.getProperty("__name");
      if (i) {
        var a = t.getTypeOfSymbol(i);
        if (a.isUnionOrIntersection()) {
          var s = a.types.find(e => e.isStringLiteral());
          return s && s.isStringLiteral() ? s.value : null;
        } else if (a.isStringLiteral()) {
          return a.value;
        }
      }
    }
    return null;
  });
};

function isValueDeclaration(e) {
  switch (e.kind) {
   case v.SyntaxKind.BinaryExpression:
   case v.SyntaxKind.ArrowFunction:
   case v.SyntaxKind.BindingElement:
   case v.SyntaxKind.ClassDeclaration:
   case v.SyntaxKind.ClassExpression:
   case v.SyntaxKind.ClassStaticBlockDeclaration:
   case v.SyntaxKind.Constructor:
   case v.SyntaxKind.EnumDeclaration:
   case v.SyntaxKind.EnumMember:
   case v.SyntaxKind.ExportAssignment:
   case v.SyntaxKind.FunctionDeclaration:
   case v.SyntaxKind.FunctionExpression:
   case v.SyntaxKind.GetAccessor:
   case v.SyntaxKind.JsxAttribute:
   case v.SyntaxKind.MethodDeclaration:
   case v.SyntaxKind.Parameter:
   case v.SyntaxKind.PropertyAssignment:
   case v.SyntaxKind.PropertyDeclaration:
   case v.SyntaxKind.SetAccessor:
   case v.SyntaxKind.ShorthandPropertyAssignment:
   case v.SyntaxKind.VariableDeclaration:
    return !0;

   default:
    return !1;
  }
}

function getValueOfValueDeclaration(e) {
  switch (e.kind) {
   case v.SyntaxKind.ClassExpression:
   case v.SyntaxKind.ClassDeclaration:
   case v.SyntaxKind.ArrowFunction:
   case v.SyntaxKind.ClassStaticBlockDeclaration:
   case v.SyntaxKind.Constructor:
   case v.SyntaxKind.EnumDeclaration:
   case v.SyntaxKind.FunctionDeclaration:
   case v.SyntaxKind.FunctionExpression:
   case v.SyntaxKind.GetAccessor:
   case v.SyntaxKind.SetAccessor:
   case v.SyntaxKind.MethodDeclaration:
    return e;

   case v.SyntaxKind.BindingElement:
   case v.SyntaxKind.EnumMember:
   case v.SyntaxKind.JsxAttribute:
   case v.SyntaxKind.Parameter:
   case v.SyntaxKind.PropertyAssignment:
   case v.SyntaxKind.PropertyDeclaration:
   case v.SyntaxKind.VariableDeclaration:
    return e.initializer;

   case v.SyntaxKind.ExportAssignment:
    return e.expression;

   case v.SyntaxKind.BinaryExpression:
    return function isAssignmentOperator(e) {
      switch (e.kind) {
       case v.SyntaxKind.EqualsToken:
       case v.SyntaxKind.BarBarEqualsToken:
       case v.SyntaxKind.AmpersandAmpersandEqualsToken:
       case v.SyntaxKind.QuestionQuestionEqualsToken:
        return !0;

       default:
        return !1;
      }
    }(e.operatorToken) ? e.right : void 0;

   case v.SyntaxKind.ShorthandPropertyAssignment:
    return e.objectAssignmentInitializer;

   default:
    return;
  }
}

function climbPastPropertyOrElementAccess(e) {
  if (e.parent && v.isPropertyAccessExpression(e.parent) && e.parent.name === e) {
    return e.parent;
  } else if (e.parent && v.isElementAccessExpression(e.parent) && e.parent.argumentExpression === e) {
    return e.parent;
  } else {
    return e;
  }
}

function getNameFromPropertyName(e) {
  if (v.isComputedPropertyName(e)) {
    return v.isStringLiteralLike(e.expression) || v.isNumericLiteral(e.expression) ? e.expression.text : void 0;
  } else if (v.isPrivateIdentifier(e) || v.isMemberName(e)) {
    return v.idText(e);
  } else {
    return e.text;
  }
}

function getDeclarationOfIdentifier(e, t) {
  var i = t.getSymbolAtLocation(e);
  if (i?.declarations?.[0] && i.flags & v.SymbolFlags.Alias && (e.parent === i?.declarations?.[0] || !v.isNamespaceImport(i.declarations[0]))) {
    var n = t.getAliasedSymbol(i);
    if (n.declarations) {
      i = n;
    }
  }
  if (i && v.isShorthandPropertyAssignment(e.parent)) {
    var a = t.getShorthandAssignmentValueSymbol(i.valueDeclaration);
    if (a) {
      i = a;
    }
  } else if (v.isBindingElement(e.parent) && v.isObjectBindingPattern(e.parent.parent) && e === (e.parent.propertyName || e.parent.name)) {
    var s = getNameFromPropertyName(e);
    var l = s ? t.getTypeAtLocation(e.parent.parent).getProperty(s) : void 0;
    if (l) {
      i = l;
    }
  } else if (v.isObjectLiteralElement(e.parent) && (v.isObjectLiteralExpression(e.parent.parent) || v.isJsxAttributes(e.parent.parent)) && e.parent.name === e) {
    var c = getNameFromPropertyName(e);
    var h = c ? t.getContextualType(e.parent.parent)?.getProperty(c) : void 0;
    if (h) {
      i = h;
    }
  }
  if (i && i.declarations?.length) {
    if (i.flags & v.SymbolFlags.Class && !(i.flags & (v.SymbolFlags.Function | v.SymbolFlags.Variable)) && function isNewExpressionTarget(e) {
      var t = climbPastPropertyOrElementAccess(e).parent;
      return v.isNewExpression(t) && t.expression === e;
    }(e)) {
      for (var f of i.declarations) {
        if (v.isClassLike(f)) {
          return f;
        }
      }
    } else if (function isCallOrNewExpressionTarget(e) {
      var t = climbPastPropertyOrElementAccess(e).parent;
      return v.isCallOrNewExpression(t) && t.expression === e;
    }(e) || function isNameOfFunctionDeclaration(e) {
      return v.isIdentifier(e) && e.parent && v.isFunctionLike(e.parent) && e.parent.name === e;
    }(e)) {
      for (var d of i.declarations) {
        if (v.isFunctionLike(d) && d.body && isValueDeclaration(d)) {
          return d;
        }
      }
    }
    if (i.valueDeclaration && v.isPropertyAccessExpression(i.valueDeclaration)) {
      var g = i.valueDeclaration.parent;
      if (g && v.isBinaryExpression(g) && g.left === i.valueDeclaration) {
        return g;
      }
    }
    if (i.valueDeclaration && isValueDeclaration(i.valueDeclaration)) {
      return i.valueDeclaration;
    }
    for (var m of i.declarations) {
      if (isValueDeclaration(m)) {
        return m;
      }
    }
  }
  return;
}

function getValueOfIdentifier(e, t) {
  while (v.isIdentifier(e)) {
    var i = getDeclarationOfIdentifier(e, t);
    if (!i) {
      return;
    } else {
      var n = getValueOfValueDeclaration(i);
      if (n && v.isIdentifier(n) && n !== e) {
        e = n;
      } else {
        return n;
      }
    }
  }
}

function resolveTemplate(e, t, i) {
  if (v.isStringLiteralLike(e)) {
    return {
      combinedText: e.getText().slice(1, -1),
      isStatic: !0,
      resolvedSpans: []
    };
  }
  if (v.isTemplateExpression(e)) {
    var n = i.languageService.getProgram()?.getTypeChecker();
    if (!n) {
      return {
        combinedText: "",
        isStatic: !1,
        resolvedSpans: []
      };
    }
    var a = e.head.text;
    var s = 0;
    var l = [];
    for (var c of e.templateSpans) {
      var h = n.getTypeAtLocation(c.expression);
      if (!(h.flags & v.TypeFlags.StringLiteral)) {
        return {
          combinedText: "",
          isStatic: !1,
          resolvedSpans: []
        };
      }
      var f = c.expression.getStart() - 2;
      var d = {
        start: f,
        length: c.expression.end - f + 1
      };
      var g = h.value;
      l.push({
        lines: g.split("\n").length,
        identifier: c.expression.getText(),
        original: d,
        new: {
          start: d.start - 1 + s,
          length: g.length
        }
      });
      s += g.length - d.length;
      a += g + c.literal.text;
    }
    return {
      combinedText: a,
      isStatic: !0,
      resolvedSpans: l
    };
  }
  var m = e.template.getText().slice(1, -1);
  if (v.isNoSubstitutionTemplateLiteral(e.template) || 0 === e.template.templateSpans.length) {
    return {
      combinedText: m,
      isStatic: !0,
      resolvedSpans: []
    };
  }
  var S = 0;
  var x = e.template.templateSpans.map(e => {
    if (v.isIdentifier(e.expression)) {
      var t = i.languageService.getProgram()?.getTypeChecker();
      if (!t) {
        return;
      }
      var n = getDeclarationOfIdentifier(e.expression, t);
      if (!n) {
        return;
      }
      var a = n;
      if (v.isVariableDeclaration(a)) {
        var s = e.expression.escapedText;
        var l = getValueOfValueDeclaration(a);
        if (!l) {
          return;
        }
        var c = e.expression.getStart() - 2;
        var h = {
          start: c,
          length: e.expression.end - c + 1
        };
        if (v.isTaggedTemplateExpression(l)) {
          var f = resolveTemplate(l, a.getSourceFile().fileName, i);
          m = m.replace("${" + e.expression.escapedText + "}", f.combinedText);
          var d = {
            lines: f.combinedText.split("\n").length,
            identifier: s,
            original: h,
            new: {
              start: h.start + S,
              length: f.combinedText.length
            }
          };
          S += f.combinedText.length - h.length;
          return d;
        } else if (v.isAsExpression(l) && v.isTaggedTemplateExpression(l.expression)) {
          var g = resolveTemplate(l.expression, a.getSourceFile().fileName, i);
          m = m.replace("${" + e.expression.escapedText + "}", g.combinedText);
          var x = {
            lines: g.combinedText.split("\n").length,
            identifier: s,
            original: h,
            new: {
              start: h.start + S,
              length: g.combinedText.length
            }
          };
          S += g.combinedText.length - h.length;
          return x;
        } else if (v.isAsExpression(l) && v.isAsExpression(l.expression) && v.isObjectLiteralExpression(l.expression.expression)) {
          var E = print(JSON.parse(l.expression.expression.getText()));
          m = m.replace("${" + e.expression.escapedText + "}", E);
          var b = {
            lines: E.split("\n").length,
            identifier: s,
            original: h,
            new: {
              start: h.start + S,
              length: E.length
            }
          };
          S += E.length - h.length;
          return b;
        }
        return;
      }
    }
    return;
  }).filter(Boolean);
  return {
    combinedText: m,
    isStatic: !0,
    resolvedSpans: x
  };
}

var resolveTadaFragmentArray = e => {
  if (!e) {
    return;
  }
  while (v.isAsExpression(e)) {
    e = e.expression;
  }
  if (!v.isArrayLiteralExpression(e)) {
    return;
  }
  if (e.elements.every(v.isIdentifier)) {
    return e.elements;
  }
  var t = [];
  for (var i of e.elements) {
    while (v.isPropertyAccessExpression(i)) {
      i = i.name;
    }
    if (v.isIdentifier(i)) {
      t.push(i);
    }
  }
  return t;
};

function getSource(e, t) {
  var i = e.languageService.getProgram();
  if (!i) {
    return;
  }
  var n = i.getSourceFile(t);
  if (!n) {
    return;
  }
  return n;
}

function findNode(e, t) {
  return function find(e) {
    if (t >= e.getStart() && t < e.getEnd()) {
      return v.forEachChild(e, find) || e;
    }
  }(e);
}

function findAllTaggedTemplateNodes(e) {
  var t = [];
  !function find(e) {
    if (isGraphQLTag(e) || v.isNoSubstitutionTemplateLiteral(e) && isGraphQLTag(e.parent)) {
      t.push(e);
      return;
    } else {
      v.forEachChild(e, find);
    }
  }(e);
  return t;
}

function unrollFragment(e, t, i) {
  var n = [];
  var a = [ e ];
  var s = new WeakSet;
  var _unrollElement = e => {
    if (s.has(e)) {
      return;
    }
    s.add(e);
    var t = function resolveIdentifierToGraphQLCall(e, t, i) {
      if (!i) {
        return null;
      }
      var n = getValueOfIdentifier(e, i);
      if (!n) {
        return null;
      }
      return isGraphQLCall(n, i) ? n : null;
    }(e, 0, i);
    if (!t) {
      return;
    }
    var c = resolveTadaFragmentArray(t.arguments[1]);
    if (c) {
      a.push(...c);
    }
    try {
      l(t.arguments[0].getText().slice(1, -1), {
        noLocation: !0
      }).definitions.forEach(e => {
        if ("FragmentDefinition" === e.kind) {
          n.push(e);
        }
      });
    } catch (e) {}
  };
  var c;
  while (void 0 !== (c = a.shift())) {
    _unrollElement(c);
  }
  return n;
}

function unrollTadaFragments(e, t, i) {
  var n = i.languageService.getProgram()?.getTypeChecker();
  e.elements.forEach(e => {
    if (v.isIdentifier(e)) {
      t.push(...unrollFragment(e, 0, n));
    } else if (v.isPropertyAccessExpression(e)) {
      if (v.isIdentifier(e.name)) {
        t.push(...unrollFragment(e.name, 0, n));
      }
    }
  });
  return t;
}

function findAllCallExpressions(e, t, i = !0) {
  var {searchExternal: n = !0, collectFragments: a = !0} = "boolean" == typeof i ? {
    searchExternal: i
  } : i;
  var s = t.languageService.getProgram()?.getTypeChecker();
  var l = [];
  var c = [];
  var h = n ? !1 : !0;
  var f = new Map;
  var unrollFragmentMemoized = e => {
    var t = s && s.getSymbolAtLocation(e);
    if (!t) {
      return unrollFragment(e, 0, s);
    }
    var i = f.get(t);
    if (!i) {
      f.set(t, i = unrollFragment(e, 0, s));
    }
    return i;
  };
  !function find(e) {
    if (!v.isCallExpression(e) || isIIFE(e)) {
      return v.forEachChild(e, find);
    }
    if (!isGraphQLCall(e, s)) {
      return v.forEachChild(e, find);
    }
    var i = getSchemaName(e, s);
    var n = e.arguments[0];
    var f = resolveTadaFragmentArray(e.arguments[1]);
    var d = isTadaGraphQLCall(e, s);
    if (!a) {} else if (!h && !f) {
      if (!d) {
        h = !0;
        c.push(...getAllFragments(e, t));
      }
    } else if (f) {
      for (var g of f) {
        c.push(...unrollFragmentMemoized(g));
      }
    }
    if (n && (v.isStringLiteralLike(n) || v.isTemplateExpression(n))) {
      l.push({
        node: n,
        schema: i,
        tadaFragmentRefs: d ? void 0 === f ? [] : f : void 0
      });
    }
  }(e);
  return {
    nodes: l,
    fragments: c
  };
}

function findAllPersistedCallExpressions(e, t) {
  var i = [];
  var n = t?.languageService.getProgram()?.getTypeChecker();
  !function find(e) {
    if (!v.isCallExpression(e) || isIIFE(e)) {
      return v.forEachChild(e, find);
    }
    if (!isTadaPersistedCall(e, n)) {
      return;
    } else if (t) {
      var a = getSchemaName(e, n, !0);
      i.push({
        node: e,
        schema: a
      });
    } else {
      i.push(e);
    }
  }(e);
  return i;
}

function getAllFragments(e, t) {
  var i = [];
  var n = t.languageService.getProgram()?.getTypeChecker();
  if (!v.isCallExpression(e)) {
    return i;
  }
  var a = resolveTadaFragmentArray(e.arguments[1]);
  if (a) {
    var s = t.languageService.getProgram()?.getTypeChecker();
    for (var c of a) {
      i.push(...unrollFragment(c, 0, s));
    }
    return i;
  } else if (isTadaGraphQLCall(e, n)) {
    return i;
  }
  if (!n) {
    return i;
  }
  var h = function getIdentifierOfChainExpression(e) {
    var t = e;
    while (t) {
      if (v.isPropertyAccessExpression(t)) {
        t = t.name;
      } else if (v.isAsExpression(t) || v.isSatisfiesExpression(t) || v.isNonNullExpression(t) || v.isParenthesizedExpression(t) || v.isExpressionWithTypeArguments(t)) {
        t = t.expression;
      } else if (v.isCommaListExpression(t)) {
        t = t.elements[t.elements.length - 1];
      } else if (v.isIdentifier(t)) {
        return t;
      } else {
        return;
      }
    }
  }(e.expression);
  if (!h) {
    return i;
  }
  var f = getDeclarationOfIdentifier(h, n);
  if (!f) {
    return i;
  }
  var d = f.getSourceFile();
  if (!d) {
    return i;
  }
  var g = [ {
    fileName: d.fileName,
    textSpan: {
      start: f.getStart(),
      length: f.getWidth()
    }
  } ];
  if (!g || !g.length) {
    return i;
  }
  var m = g[0];
  if (!m) {
    return i;
  }
  var S = getSource(t, m.fileName);
  if (!S) {
    return i;
  }
  v.forEachChild(S, e => {
    if (v.isVariableStatement(e) && e.declarationList && e.declarationList.declarations[0] && "documents" === e.declarationList.declarations[0].name.getText()) {
      var [t] = e.declarationList.declarations;
      if (t.initializer && v.isObjectLiteralExpression(t.initializer)) {
        t.initializer.properties.forEach(e => {
          if (v.isPropertyAssignment(e) && v.isStringLiteral(e.name)) {
            try {
              var t = JSON.parse(`${e.name.getText().replace(/'/g, '"')}`);
              if (t.includes("fragment ") && t.includes(" on ")) {
                l(t, {
                  noLocation: !0
                }).definitions.forEach(e => {
                  if ("FragmentDefinition" === e.kind) {
                    i.push(e);
                  }
                });
              }
            } catch (e) {}
          }
        });
      }
    }
  });
  return i;
}

function findAllImports(e) {
  return e.statements.filter(v.isImportDeclaration);
}

function bubbleUpTemplate(e) {
  while (v.isNoSubstitutionTemplateLiteral(e) || v.isToken(e) || v.isTemplateExpression(e) || v.isTemplateSpan(e)) {
    e = e.parent;
  }
  return e;
}

function bubbleUpCallExpression(e) {
  while (v.isStringLiteralLike(e) || v.isToken(e) || v.isTemplateExpression(e) || v.isTemplateSpan(e)) {
    e = e.parent;
  }
  return e;
}

var Q = 52005;

var unwrapAbstractType = e => e.isUnionOrIntersection() ? e.types.find(e => e.flags & v.TypeFlags.Object) || e : e;

var getVariableDeclaration = e => {
  var t = e;
  var i = new Set;
  while (t.parent && !i.has(t)) {
    i.add(t);
    if (v.isBlock(t)) {
      return;
    } else if (v.isVariableDeclaration(t = t.parent)) {
      return t;
    }
  }
};

var q = new Set([ "map", "filter", "forEach", "reduce", "every", "some", "find", "flatMap", "sort" ]);

var resolveDataType = (e, t) => {
  var i;
  var n = t.getTypeAtLocation(e.parent);
  if ("target" in n) {
    var a = n.resolvedTypeArguments;
    i = a && a.length > 1 ? a[0] : void 0;
  }
  if (!i) {
    var s = n.getProperty("__apiType");
    if (s) {
      var l = t.getTypeOfSymbol(s);
      var c = n.getCallSignatures()[0];
      if (l.isUnionOrIntersection()) {
        for (var h of l.types) {
          if (c = h.getCallSignatures()[0]) {
            i = c.getReturnType();
            break;
          }
        }
      }
      i = c && c.getReturnType();
    }
  }
  return i;
};

function getFragmentsInSource(t, i, n) {
  var a = [];
  var s = findAllCallExpressions(t, n, !1);
  var c = i.getSymbolAtLocation(t);
  if (!c) {
    return [];
  }
  var h = i.getExportsOfModule(c).map(e => e.name);
  s.nodes.filter(e => {
    var t = e.node.parent;
    while (t && !v.isSourceFile(t) && !v.isVariableDeclaration(t)) {
      t = t.parent;
    }
    if (v.isVariableDeclaration(t)) {
      return h.includes(t.name.getText());
    } else {
      return !1;
    }
  }).forEach(t => {
    var i = resolveTemplate(t.node, 0, n).combinedText;
    try {
      var s = l(i, {
        noLocation: !0
      });
      if (s.definitions.every(t => t.kind === e.FRAGMENT_DEFINITION)) {
        a = a.concat(s.definitions);
      }
    } catch (e) {
      return;
    }
  });
  return a;
}

function getPersistedCodeFixAtPosition(e, t, i) {
  var n = i.config.templateIsCallExpression ?? !0;
  var a = i.languageService.getProgram()?.getTypeChecker();
  if (!n) {
    return;
  }
  var s = getSource(i, e);
  if (!s) {
    return;
  }
  var l = findNode(s, t);
  if (!l) {
    return;
  }
  var c = l;
  if (v.isVariableStatement(c)) {
    c = c.declarationList.declarations.find(e => v.isVariableDeclaration(e) && e.initializer && v.isCallExpression(e.initializer)) || l;
  } else if (v.isVariableDeclarationList(c)) {
    c = c.declarations.find(e => v.isVariableDeclaration(e) && e.initializer && v.isCallExpression(e.initializer)) || l;
  } else if (v.isVariableDeclaration(c) && c.initializer && v.isCallExpression(c.initializer)) {
    c = c.initializer;
  } else {
    while (c && !v.isCallExpression(c)) {
      c = c.parent;
    }
  }
  if (!isTadaPersistedCall(c, a)) {
    return;
  }
  var h, f = e;
  if (c.typeArguments) {
    var [d] = c.typeArguments;
    if (!d || !v.isTypeQueryNode(d)) {
      return;
    }
    var {node: g, filename: m} = getDocumentReferenceFromTypeQuery(d, e, i);
    h = g;
    f = m;
  } else if (c.arguments[1]) {
    if (!v.isIdentifier(c.arguments[1]) && !v.isCallExpression(c.arguments[1])) {
      return;
    }
    var {node: S, filename: x} = getDocumentReferenceFromDocumentNode(c.arguments[1], e, i);
    h = S;
    f = x;
  }
  if (!h) {
    return;
  }
  var E = h;
  var b = v.isCallExpression(E) && E.arguments[0];
  if (!(E && v.isCallExpression(E) && b && (v.isStringLiteralLike(b) || v.isTemplateExpression(b) && resolveTemplate(b, 0, i).isStatic))) {
    return;
  }
  var A = generateHashForDocument(i, b, f, E.arguments[1] && v.isArrayLiteralExpression(E.arguments[1]) ? E.arguments[1] : void 0);
  var w = c.arguments[0];
  if (!w) {
    return {
      span: {
        start: c.arguments.pos,
        length: 1
      },
      replacement: `"sha256:${A}")`
    };
  } else if (v.isStringLiteral(w) && w.getText() !== `"sha256:${A}"`) {
    return {
      span: {
        start: w.getStart(),
        length: w.end - w.getStart()
      },
      replacement: `"sha256:${A}"`
    };
  } else if (v.isIdentifier(w)) {
    return {
      span: {
        start: w.getStart(),
        length: w.end - w.getStart()
      },
      replacement: `"sha256:${A}"`
    };
  } else {
    return;
  }
}

var generateHashForDocument = (e, t, i, n) => {
  if (n) {
    var a = [];
    unrollTadaFragments(n, a, e);
    var s = resolveTemplate(t, 0, e).combinedText;
    var l = parse(s);
    var c = new Set;
    for (var h of l.definitions) {
      if (h.kind === I && !c.has(h)) {
        stripUnmaskDirectivesFromDefinition(h);
      }
    }
    a.map(e => {
      stripUnmaskDirectivesFromDefinition(e);
      return print(e);
    }).filter((e, t, i) => i.indexOf(e) === t).forEach(e => {
      s = `${s}\n\n${e}`;
    });
    var f = print(parse(s));
    return d("sha256").update(f).digest("hex");
  } else {
    var v = getSource(e, i);
    var {fragments: g} = findAllCallExpressions(v, e);
    var m = resolveTemplate(t, 0, e).combinedText;
    var S = parse(m);
    var x = new Set;
    for (var E of S.definitions) {
      if (E.kind === I && !x.has(E)) {
        stripUnmaskDirectivesFromDefinition(E);
      }
    }
    var b = new Set;
    visit(S, {
      FragmentDefinition: e => {
        g.push(e);
      },
      FragmentSpread: e => {
        b.add(e.name.value);
      }
    });
    var A = m;
    var w = new Set;
    var C = [ ...b ];
    var D;
    while (D = C.shift()) {
      w.add(D);
      var F = g.find(e => e.name.value === D);
      if (!F) {
        e.project.projectService.logger.info(`[GraphQLSP] could not find fragment for spread ${D}!`);
        return;
      }
      stripUnmaskDirectivesFromDefinition(F);
      visit(F, {
        FragmentSpread: e => {
          if (!w.has(e.name.value)) {
            C.push(e.name.value);
          }
        }
      });
      A = `${A}\n\n${print(F)}`;
    }
    return d("sha256").update(print(parse(A))).digest("hex");
  }
};

var getDocumentReferenceFromTypeQuery = (e, t, i) => {
  var n = i.languageService.getProgram()?.getTypeChecker();
  if (!n) {
    return {
      node: null,
      filename: t
    };
  }
  var a;
  if (v.isIdentifier(e.exprName)) {
    a = e.exprName;
  } else if (v.isQualifiedName(e.exprName)) {
    a = e.exprName.right;
  }
  if (!a) {
    return {
      node: null,
      filename: t
    };
  }
  var s = getValueOfIdentifier(a, n);
  if (!s || !isGraphQLCall(s, n)) {
    return {
      node: null,
      filename: t
    };
  }
  return {
    node: s,
    filename: s.getSourceFile().fileName
  };
};

var getDocumentReferenceFromDocumentNode = (e, t, i) => {
  if (v.isIdentifier(e)) {
    var n = i.languageService.getProgram()?.getTypeChecker();
    if (!n) {
      return {
        node: null,
        filename: t
      };
    }
    var a = getValueOfIdentifier(e, n);
    if (!a || !isGraphQLCall(a, n)) {
      return {
        node: null,
        filename: t
      };
    }
    return {
      node: a,
      filename: a.getSourceFile().fileName
    };
  } else {
    return {
      node: e,
      filename: t
    };
  }
};

var stripUnmaskDirectivesFromDefinition = e => {
  e.directives = e.directives?.filter(e => "_unmask" !== e.name.value);
};

var J = new Set([ "populate", "client", "unmask", "_unmask", "_optional", "_relayPagination", "_simplePagination", "_required", "optional", "required", "arguments", "argumentDefinitions", "connection", "refetchable", "relay", "required", "inline" ]);

var X = 520100;

var Y = 520101;

var H = 520102;

var Z = 520103;

var ee = [ 52001, 52004, 52003, Q, 52006, 52007, 52008, 52009, X, Y, H, Z ];

var getMisconfigurationDiagnostics = (e, t, i, n) => {
  if (!i.errors || !t.length) {
    return [];
  }
  var a = [];
  var s = t[0].node;
  var l = [ i.errors.config, ...i.errors.load.values(), ...i.errors.write.values() ].filter(Boolean);
  var c = n.languageService.getProgram();
  if (c) {
    for (var [h, f] of i.outputLocations) {
      if (Date.now() - f > 3e4 && !c.getSourceFile(h)) {
        l.push(`The generated typings file "${h}" is not part of the TypeScript project. Check that it is matched by the "include" patterns of your tsconfig.json.`);
      }
    }
  }
  for (var d of l) {
    a.push({
      category: v.DiagnosticCategory.Error,
      code: 52006,
      file: e,
      messageText: d,
      start: s.getStart(),
      length: s.getEnd() - s.getStart()
    });
  }
  if (!!i.current || Object.values(i.multi).some(Boolean)) {
    var g = Object.keys(i.multi);
    for (var {node: m, schema: S} of t) {
      if (S && !g.includes(S)) {
        a.push({
          category: v.DiagnosticCategory.Error,
          code: 52008,
          file: e,
          messageText: `This document refers to the schema named "${S}", which isn't configured. ` + (g.length ? `Configured schemas are: ${g.join(", ")}.` : 'No named schemas are configured in the "schemas" option.'),
          start: m.getStart(),
          length: m.getEnd() - m.getStart()
        });
      }
    }
  }
  return a;
};

var te = new k({
  ttl: 9e5,
  max: 5e3
});

function getGraphQLDiagnostics(t, i, n) {
  var a = n.config.templateIsCallExpression ?? !0;
  var s = getSource(n, t);
  if (!s) {
    return;
  }
  var c, h = [];
  if (a) {
    var d = findAllCallExpressions(s, n);
    h = d.fragments;
    c = d.nodes;
  } else {
    c = findAllTaggedTemplateNodes(s).map(e => ({
      node: e,
      schema: null
    }));
  }
  var g = c.map(({node: e}) => {
    if ((v.isNoSubstitutionTemplateLiteral(e) || v.isTemplateExpression(e)) && !a) {
      if (v.isTaggedTemplateExpression(e.parent)) {
        e = e.parent;
      } else {
        return;
      }
    }
    return resolveTemplate(e, 0, n).combinedText;
  });
  var m = fnv1a(a ? s.getText() + h.map(e => print(e)).join("-") + i.version : g.join("-") + i.version);
  var S;
  if (te.has(m)) {
    S = te.get(m);
  } else {
    S = runDiagnostics(s, {
      nodes: c,
      fragments: h
    }, i, n);
    te.set(m, S);
  }
  S = [ ...getMisconfigurationDiagnostics(s, c, i, n), ...S ];
  if (!c.length) {
    var x = ((e, t, i) => {
      if (!new RegExp(`\\b(?:${Array.from(B).join("|")})\\s*${t ? "`" : "\\("}`).test(e.getText())) {
        return null;
      }
      var n;
      var a;
      if (t) {
        n = findAllTaggedTemplateNodes(e)[0];
        a = 'Found GraphQL documents in tagged templates, but GraphQLSP is configured to search for graphql()/gql() calls. If you use tagged templates, set "templateIsCallExpression": false in the plugin configuration in your tsconfig.json.';
      } else {
        n = findAllCallExpressions(e, i, {
          searchExternal: !1,
          collectFragments: !1
        }).nodes.find(e => {
          var t = v.isTemplateExpression(e.node) ? e.node.head.text : e.node.text;
          return /^[\s,]*(?:query|mutation|subscription|fragment|\{)/.test(t);
        })?.node;
        a = 'Found GraphQL documents in graphql()/gql() calls, but GraphQLSP is configured to search for tagged templates. If you use call expressions, remove "templateIsCallExpression": false from the plugin configuration in your tsconfig.json.';
      }
      if (!n) {
        return null;
      }
      return {
        category: v.DiagnosticCategory.Warning,
        code: 52007,
        file: e,
        messageText: a,
        start: n.getStart(),
        length: n.getEnd() - n.getStart()
      };
    })(s, a, n);
    if (x) {
      S.unshift(x);
    }
  }
  var E = n.config.shouldCheckForColocatedFragments ?? !0;
  var b = [];
  if (a) {
    var A = findAllPersistedCallExpressions(s, n).map(e => {
      var {node: i} = e;
      if (!i.typeArguments && !i.arguments[1]) {
        return {
          category: v.DiagnosticCategory.Warning,
          code: X,
          file: s,
          messageText: "Missing generic pointing at the GraphQL document.",
          start: i.getStart(),
          length: i.getEnd() - i.getStart()
        };
      }
      var a, l, c, h, f = t;
      var d = i.typeArguments && i.typeArguments[0];
      if (d) {
        c = d.getStart();
        h = d.getEnd() - d.getStart();
        if (!v.isTypeQueryNode(d)) {
          return {
            category: v.DiagnosticCategory.Warning,
            code: X,
            file: s,
            messageText: "Provided generic should be a typeQueryNode in the shape of graphql.persisted<typeof document>.",
            start: c,
            length: h
          };
        }
        var {node: g, filename: m} = getDocumentReferenceFromTypeQuery(d, t, n);
        a = g;
        f = m;
        l = d.getText();
      } else if (i.arguments[1]) {
        c = i.arguments[1].getStart();
        h = i.arguments[1].getEnd() - i.arguments[1].getStart();
        if (!v.isIdentifier(i.arguments[1]) && !v.isCallExpression(i.arguments[1])) {
          return {
            category: v.DiagnosticCategory.Warning,
            code: X,
            file: s,
            messageText: 'Provided argument should be an identifier or invocation of "graphql" in the shape of graphql.persisted(hash, document).',
            start: c,
            length: h
          };
        }
        var {node: S, filename: x} = getDocumentReferenceFromDocumentNode(i.arguments[1], t, n);
        a = S;
        f = x;
        l = i.arguments[1].getText();
      }
      if (!a) {
        return {
          category: v.DiagnosticCategory.Warning,
          code: H,
          file: s,
          messageText: `Can't find reference to "${l}".`,
          start: c,
          length: h
        };
      }
      var E = a;
      var b = v.isCallExpression(E) && E.arguments[0];
      if (!(E && v.isCallExpression(E) && b && (v.isStringLiteralLike(b) || v.isTemplateExpression(b) && resolveTemplate(b, 0, n).isStatic))) {
        return {
          category: v.DiagnosticCategory.Warning,
          code: H,
          file: s,
          messageText: `Referenced type "${l}" is not a GraphQL document.`,
          start: c,
          length: h
        };
      }
      if (!i.arguments[0]) {
        return {
          category: v.DiagnosticCategory.Warning,
          code: Y,
          file: s,
          messageText: "The call-expression is missing a hash for the persisted argument.",
          start: i.arguments.pos,
          length: i.arguments.end - i.arguments.pos
        };
      }
      var A = i.arguments[0].getText().slice(1, -1);
      if (A.startsWith("sha256:")) {
        var w = generateHashForDocument(n, b, f, E.arguments[1] && v.isArrayLiteralExpression(E.arguments[1]) ? E.arguments[1] : void 0);
        if (!w) {
          return null;
        }
        if (`sha256:${w}` !== A) {
          return {
            category: v.DiagnosticCategory.Warning,
            code: Z,
            file: s,
            messageText: "The persisted document's hash is outdated",
            start: i.arguments.pos,
            length: i.arguments.end - i.arguments.pos
          };
        }
      }
      return null;
    }).filter(Boolean);
    S.push(...A);
  }
  if (a && E) {
    var w = ((e, t) => {
      var i = findAllImports(e);
      var n = t.languageService.getProgram()?.getTypeChecker();
      var a = {};
      if (!n) {
        return a;
      }
      if (i.length) {
        i.forEach(e => {
          if (!e.importClause) {
            return;
          }
          if (e.importClause.name) {
            var i = getDeclarationOfIdentifier(e.importClause.name, n);
            if (i) {
              var s = i.getSourceFile();
              if (s.fileName.includes("node_modules")) {
                return;
              }
              if (!s) {
                return;
              }
              var l = getFragmentsInSource(s, n, t).map(e => e.name.value);
              var c = e.moduleSpecifier.getText();
              var h = a[c];
              if (l.length && h) {
                h.fragments = h.fragments.concat(l);
              } else if (l.length && !h) {
                a[c] = h = {
                  start: e.moduleSpecifier.getStart(),
                  length: e.moduleSpecifier.getText().length,
                  fragments: l
                };
              }
            }
          }
          if (e.importClause.namedBindings && v.isNamespaceImport(e.importClause.namedBindings)) {
            var f = getDeclarationOfIdentifier(e.importClause.namedBindings.name, n);
            if (f) {
              var d = f.getSourceFile();
              if (d.fileName.includes("node_modules")) {
                return;
              }
              if (!d) {
                return;
              }
              var g = getFragmentsInSource(d, n, t).map(e => e.name.value);
              var m = e.moduleSpecifier.getText();
              var S = a[m];
              if (g.length && S) {
                S.fragments = S.fragments.concat(g);
              } else if (g.length && !S) {
                a[m] = S = {
                  start: e.moduleSpecifier.getStart(),
                  length: e.moduleSpecifier.getText().length,
                  fragments: g
                };
              }
            }
          } else if (e.importClause.namedBindings && v.isNamedImportBindings(e.importClause.namedBindings)) {
            e.importClause.namedBindings.elements.forEach(i => {
              var s = i.name || i.propertyName;
              if (!s) {
                return;
              }
              var l = getDeclarationOfIdentifier(s, n);
              if (l) {
                var c = l.getSourceFile();
                if (c.fileName.includes("node_modules")) {
                  return;
                }
                if (!c) {
                  return;
                }
                var h = getFragmentsInSource(c, n, t).map(e => e.name.value);
                var f = e.moduleSpecifier.getText();
                var d = a[f];
                if (h.length && d) {
                  d.fragments = d.fragments.concat(h);
                } else if (h.length && !d) {
                  a[f] = d = {
                    start: e.moduleSpecifier.getStart(),
                    length: e.moduleSpecifier.getText().length,
                    fragments: h
                  };
                }
              }
            });
          }
        });
      }
      return a;
    })(s, n);
    var C = n.languageService.getProgram()?.getTypeChecker();
    var D = new Set;
    c.forEach(({node: e}) => {
      try {
        var t = resolveTemplate(e, 0, n);
        if (!t.isStatic) {
          return;
        }
        var i = l(t.combinedText, {
          noLocation: !0
        });
        f(i, {
          FragmentSpread: e => {
            D.add(e.name.value);
          }
        });
      } catch (e) {}
    });
    var F = function getDirectlyUsedFragments(t, i, n, a) {
      var s = new Set;
      if (!n) {
        return s;
      }
      var c = new Set;
      i.forEach(({tadaFragmentRefs: e}) => {
        if (e) {
          e.forEach(e => c.add(e));
        }
      });
      var h = new Map;
      var visit = e => {
        if (v.isIdentifier(e)) {
          var t = n.getSymbolAtLocation(e);
          if (t) {
            var i = h.get(t);
            if (i) {
              i.push(e);
            } else {
              h.set(t, [ e ]);
            }
          }
        }
        v.forEachChild(e, visit);
      };
      visit(t);
      var f = [];
      findAllImports(t).forEach(e => {
        var t = e.importClause;
        if (!t) {
          return;
        }
        if (t.name) {
          f.push(t.name);
        }
        var i = t.namedBindings;
        if (i) {
          if (v.isNamespaceImport(i)) {
            f.push(i.name);
          } else {
            i.elements.forEach(e => f.push(e.name));
          }
        }
      });
      f.forEach(t => {
        var i = n.getSymbolAtLocation(t);
        if (!i) {
          return;
        }
        if (!(h.get(i) || []).some(e => e !== t && !c.has(e))) {
          return;
        }
        (function getFragmentNamesForIdentifier(t, i, n) {
          var a = getValueOfIdentifier(t, i);
          if (!a || !v.isCallExpression(a)) {
            return [];
          }
          var s = a.arguments[0];
          if (!s || !v.isStringLiteralLike(s) && !v.isTemplateExpression(s)) {
            return [];
          }
          try {
            var c = resolveTemplate(s, s.getSourceFile(), n);
            if (!c.isStatic) {
              return [];
            }
            return l(c.combinedText, {
              noLocation: !0
            }).definitions.filter(t => t.kind === e.FRAGMENT_DEFINITION).map(e => e.name.value);
          } catch (e) {
            return [];
          }
        })(t, n, a).forEach(e => s.add(e));
      });
      return s;
    }(s, c, C, n);
    Object.keys(w).forEach(e => {
      var {fragments: t, start: i, length: n} = w[e];
      var a = Array.from(new Set(t.filter(e => !D.has(e) && !F.has(e))));
      if (a.length) {
        b.push({
          file: s,
          length: n,
          start: i,
          category: v.DiagnosticCategory.Warning,
          code: 52003,
          messageText: `Unused co-located fragment definition(s) "${a.join(", ")}" in ${e}`
        });
      }
    });
    return [ ...S, ...b ];
  } else {
    return S;
  }
}

var runDiagnostics = (t, {nodes: i, fragments: n}, a, s) => {
  var c = s.config.templateIsCallExpression ?? !0;
  var h = s.languageService.getProgram()?.getTypeChecker();
  var d = [];
  var g = i.map(i => {
    var f = i.node;
    if (!c && (v.isNoSubstitutionTemplateLiteral(f) || v.isTemplateExpression(f))) {
      if (v.isTaggedTemplateExpression(f.parent)) {
        f = f.parent;
      } else {
        return;
      }
    }
    var {combinedText: g, isStatic: m, resolvedSpans: S} = resolveTemplate(f, 0, s);
    if (v.isTemplateExpression(f) && !m) {
      d.push({
        category: v.DiagnosticCategory.Warning,
        code: 52009,
        file: t,
        messageText: "GraphQL documents with non-static template interpolation cannot be validated.",
        start: f.getStart(),
        length: f.getWidth()
      });
      return;
    }
    var x = g.split("\n");
    var E = !1;
    if (v.isAsExpression(f.parent)) {
      if (v.isExpressionStatement(f.parent.parent)) {
        E = !0;
      }
    } else if (v.isExpressionStatement(f.parent)) {
      E = !0;
    }
    var b = f.getStart() + (c ? 0 : f.tag.getText().length + (E ? 2 : 0));
    var A = b + f.getText().length;
    var w = [ ...n ];
    if (null != i.tadaFragmentRefs) {
      var C = new Set;
      for (var D of i.tadaFragmentRefs) {
        unrollFragment(D, 0, h).forEach(e => C.add(e.name.value));
      }
      w = w.filter(e => C.has(e.name.value));
    }
    if (c) {
      try {
        var F = l(g, {
          noLocation: !0
        }).definitions.filter(t => t.kind === e.FRAGMENT_DEFINITION);
        w = w.filter(t => !F.some(i => i.kind === e.FRAGMENT_DEFINITION && i.name.value === t.name.value));
      } catch (e) {}
    }
    var k = i.schema && a.multi[i.schema] ? a.multi[i.schema]?.schema : a.current?.schema;
    if (!k) {
      return;
    }
    var N = new Set([ ...J, ...s.config.clientDirectives || [] ]);
    var _ = getDiagnostics(g, k, 0, 0, w).filter(e => {
      if (!e.message.includes("Unknown directive")) {
        return !0;
      }
      var [t] = e.message.split("(");
      var i = t && /Unknown directive "@([^)]+)"/g.exec(t);
      if (!i) {
        return !0;
      }
      var n = i[1];
      return n && !N.has(n);
    }).map(e => {
      var {start: t, end: i} = e.range;
      var n = b + t.line;
      for (var a = 0; a <= t.line && a < x.length; a++) {
        if (a === t.line) {
          n += t.character;
        } else if (x[a]) {
          n += x[a].length;
        }
      }
      var s = b + i.line;
      for (var l = 0; l <= i.line && l < x.length; l++) {
        if (l === i.line) {
          s += i.character;
        } else if (x[l]) {
          s += x[l].length;
        }
      }
      var c = S.find(e => n >= e.new.start && n < e.new.start + e.new.length);
      if (c) {
        return {
          ...e,
          start: c.original.start,
          length: c.original.length
        };
      } else {
        var h = S.filter(e => e.new.start + e.new.length <= n).reduce((e, t) => e + (t.new.length - t.original.length), 0);
        n -= h;
        s -= h;
        return {
          ...e,
          start: n + 1,
          length: s - n
        };
      }
    }).filter(e => e.start + e.length <= A);
    return _;
  }).flat().filter(Boolean);
  var m = g.map(e => ({
    file: t,
    length: e.length,
    start: e.start,
    category: 2 === e.severity ? v.DiagnosticCategory.Warning : v.DiagnosticCategory.Error,
    code: "number" == typeof e.code ? e.code : 2 === e.severity ? 52004 : 52001,
    messageText: e.message.split("\n")[0]
  }));
  if (c) {
    var S = ((e, t, i) => {
      var n = [];
      if (!(i.config.trackFieldUsage ?? 1)) {
        return n;
      }
      var a = new Set([ "id", "_id", "__typename", ...i.config.reservedKeys ?? [] ]);
      var s = i.languageService.getProgram()?.getTypeChecker();
      if (!s) {
        return;
      }
      try {
        var c = 0;
        var makeTrieNode = e => ({
          id: c++,
          children: new Map,
          isLeaf: e,
          used: !1
        });
        var h = [];
        var _loop = function() {
          var e = d.getText();
          if (e.includes("mutation") || e.includes("subscription")) {
            return 0;
          }
          var t;
          try {
            t = l(e.slice(1, -1));
          } catch (e) {
            return 0;
          }
          var i = makeTrieNode(!1);
          var n = [];
          var s = new Map;
          var c = new Map;
          var v = [];
          var g = [ i ];
          f(t, {
            Field: {
              enter(e) {
                var t = e.alias ? e.alias.value : e.name.value;
                var i = v.length ? `${v.join(".")}.${t}` : t;
                var l = g[g.length - 1];
                if (!e.selectionSet && !a.has(e.name.value)) {
                  n.push(i);
                  c.set(i, {
                    start: e.name.loc.start,
                    length: e.name.loc.end - e.name.loc.start
                  });
                  var h = l.children.get(t);
                  if (!h) {
                    l.children.set(t, h = makeTrieNode(!0));
                  } else {
                    h.isLeaf = !0;
                  }
                  s.set(i, h);
                } else if (e.selectionSet) {
                  v.push(t);
                  c.set(i, {
                    start: e.name.loc.start,
                    length: e.name.loc.end - e.name.loc.start
                  });
                  var f = l.children.get(t);
                  if (!f) {
                    l.children.set(t, f = makeTrieNode(!1));
                  }
                  g.push(f);
                }
              },
              leave(e) {
                if (e.selectionSet) {
                  v.pop();
                  g.pop();
                }
              }
            }
          });
          h.push({
            root: i,
            allPaths: n,
            leafByPath: s,
            fieldToLoc: c,
            templateNode: d,
            accessCount: 0
          });
        };
        for (var d of t) {
          if (0 === _loop()) {
            continue;
          }
        }
        if (!h.length) {
          return n;
        }
        var g = new Map;
        var m = [];
        var indexWalk = e => {
          if (v.isTypeNode(e)) {
            return;
          }
          if (v.isIdentifier(e)) {
            var t = e.parent;
            var i = t && (v.isVariableDeclaration(t) || v.isBindingElement(t) || v.isParameter(t) || v.isFunctionDeclaration(t) || v.isFunctionExpression(t) || v.isClassDeclaration(t) || v.isImportSpecifier(t) || v.isImportClause(t) || v.isNamespaceImport(t) || v.isExportSpecifier(t)) && t.name === e;
            var n = t && (v.isPropertyAccessExpression(t) && t.name === e || v.isQualifiedName(t) && t.right === e || v.isPropertyAssignment(t) && t.name === e || v.isBindingElement(t) && t.propertyName === e || v.isJsxAttribute(t) && t.name === e || v.isPropertyDeclaration(t) && t.name === e || v.isMethodDeclaration(t) && t.name === e || v.isPropertySignature(t) && t.name === e || v.isMethodSignature(t) && t.name === e || v.isEnumMember(t) && t.name === e);
            if (!i && !n) {
              var a = g.get(e.text);
              if (!a) {
                g.set(e.text, a = []);
              }
              a.push(e);
            }
            return;
          }
          if (v.isVariableDeclaration(e) && e.initializer) {
            var s = e.initializer;
            while (v.isParenthesizedExpression(s) || v.isAwaitExpression(s) || v.isNonNullExpression(s) || v.isAsExpression(s)) {
              s = s.expression;
            }
            if (v.isCallExpression(s)) {
              m.push(e);
            }
          }
          v.forEachChild(e, indexWalk);
        };
        indexWalk(e);
        var S = new Map;
        var getRefSymbol = e => {
          if (S.has(e)) {
            return S.get(e);
          }
          var t = v.isShorthandPropertyAssignment(e.parent) && e.parent.name === e ? s.getShorthandAssignmentValueSymbol(e.parent) : s.getSymbolAtLocation(e);
          S.set(e, t);
          return t;
        };
        var x = [];
        var E = new Map;
        var addEntry = (e, t, i, n) => {
          if (!e) {
            return;
          }
          var a = E.get(e);
          if (!a) {
            E.set(e, a = new Set);
          }
          var s = `${t.id}${n ? "s" : ""}`;
          if (a.has(s)) {
            return;
          }
          a.add(s);
          x.push({
            symbol: e,
            node: t,
            doc: i,
            suppressReturnBail: n
          });
        };
        var trackBinding = (e, t, i, n) => {
          if (v.isIdentifier(e)) {
            addEntry(s.getSymbolAtLocation(e), t, i, n);
          } else if (v.isObjectBindingPattern(e)) {
            for (var a of e.elements) {
              var l = void 0;
              if (a.propertyName) {
                if (v.isIdentifier(a.propertyName) || v.isStringLiteral(a.propertyName)) {
                  l = a.propertyName.text;
                }
              } else if (v.isIdentifier(a.name)) {
                l = a.name.text;
              }
              var c = l && t.children.get(l) || t;
              trackBinding(a.name, c, i, n);
            }
          } else {
            for (var h of e.elements) {
              if (v.isOmittedExpression(h)) {
                continue;
              }
              trackBinding(h.name, t, i, n);
            }
          }
        };
        var markSubtreeUsed = e => {
          for (var t of e.children.values()) {
            t.used = !0;
            markSubtreeUsed(t);
          }
        };
        var handleArrayChain = (e, t, i, n) => {
          var a = e;
          while (1) {
            var l = a.expression.name.text;
            var c = a.arguments[0];
            if (c && v.isIdentifier(c)) {
              var h = getValueOfIdentifier(c, s);
              if (h && (v.isFunctionDeclaration(h) || v.isFunctionExpression(h) || v.isArrowFunction(h))) {
                c = h;
              }
            }
            if (c && (v.isFunctionDeclaration(c) || v.isFunctionExpression(c) || v.isArrowFunction(c))) {
              var f = c.parameters["reduce" === l ? 1 : 0];
              if (f) {
                trackBinding(f.name, i, n, !0);
              }
            }
            var d = a.parent;
            if (v.isPropertyAccessExpression(d) && d.expression === a && q.has(d.name.text) && v.isCallExpression(d.parent) && d.parent.expression === d) {
              a = d.parent;
              continue;
            }
            break;
          }
          if (v.isVariableDeclaration(e.parent) && "some" !== t && "every" !== t) {
            trackBinding(e.parent.name, i, n, !0);
          }
        };
        var walkUseChain = (e, t) => {
          var i = t.doc;
          var n = e;
          var a = t.node;
          var l = !0;
          while (1) {
            var c = n.parent;
            if (!c) {
              break;
            }
            if (v.isPropertyAccessExpression(c)) {
              if (c.expression !== n) {
                break;
              }
              var h = c.name.text;
              var f = c.parent;
              if (v.isCallExpression(f) && f.expression === c) {
                if ("at" === h) {
                  n = f;
                  continue;
                } else if (q.has(h)) {
                  handleArrayChain(f, h, a, i);
                  i.accessCount++;
                  return;
                }
              }
              a = a.children.get(h) || a;
              n = c;
              continue;
            }
            if (v.isElementAccessExpression(c)) {
              if (c.expression !== n) {
                break;
              }
              if (v.isStringLiteral(c.argumentExpression)) {
                a = a.children.get(c.argumentExpression.text) || a;
              }
              n = c;
              continue;
            }
            if (v.isNonNullExpression(c) || v.isParenthesizedExpression(c) || v.isAsExpression(c) || v.isAwaitExpression(c) || v.isSatisfiesExpression && v.isSatisfiesExpression(c)) {
              n = c;
              continue;
            }
            if (v.isBinaryExpression(c)) {
              if (c.operatorToken.kind === v.SyntaxKind.EqualsToken) {
                if (c.left === n) {
                  return;
                }
                if (v.isIdentifier(c.left)) {
                  addEntry(s.getSymbolAtLocation(c.left), a, i, !1);
                  i.accessCount++;
                  return;
                }
                if (l) {
                  if (a.isLeaf) {
                    a.used = !0;
                  }
                  markSubtreeUsed(a);
                  i.accessCount++;
                  return;
                }
                break;
              }
              if (c.operatorToken.kind !== v.SyntaxKind.BarBarToken && c.operatorToken.kind !== v.SyntaxKind.QuestionQuestionToken) {
                l = !1;
              }
              n = c;
              continue;
            }
            if (v.isConditionalExpression(c)) {
              if (c.condition === n) {
                break;
              }
              l = !1;
              n = c;
              continue;
            }
            if (v.isVariableDeclaration(c)) {
              trackBinding(c.name, a, i, !1);
              return;
            }
            if (v.isForOfStatement(c) && c.expression === n) {
              if (v.isVariableDeclarationList(c.initializer)) {
                for (var d of c.initializer.declarations) {
                  trackBinding(d.name, a, i, !1);
                }
              } else if (v.isIdentifier(c.initializer)) {
                addEntry(s.getSymbolAtLocation(c.initializer), a, i, !1);
              }
              i.accessCount++;
              return;
            }
            if (v.isReturnStatement(c) || v.isArrowFunction(c) && c.body === n) {
              if (a.isLeaf) {
                a.used = !0;
              }
              if (!t.suppressReturnBail) {
                markSubtreeUsed(a);
              }
              i.accessCount++;
              return;
            }
            if (v.isCallExpression(c) && c.expression !== n) {
              if (a.isLeaf) {
                a.used = !0;
              }
              if (l) {
                markSubtreeUsed(a);
              }
              i.accessCount++;
              return;
            }
            if (l && (v.isSpreadElement(c) || v.isSpreadAssignment(c) || v.isJsxSpreadAttribute(c) || v.isShorthandPropertyAssignment(c) || v.isPropertyAssignment(c) && c.initializer === n)) {
              if (a.isLeaf) {
                a.used = !0;
              }
              markSubtreeUsed(a);
              i.accessCount++;
              return;
            }
            break;
          }
          if (a.isLeaf) {
            a.used = !0;
          }
          i.accessCount++;
        };
        var b = new Map;
        for (var A of h) {
          var w = A.templateNode;
          var C = void 0;
          var D = w.parent.parent;
          while (v.isParenthesizedExpression(D) || v.isAsExpression(D) || v.isNonNullExpression(D) || v.isSatisfiesExpression && v.isSatisfiesExpression(D)) {
            D = D.parent;
          }
          if (v.isVariableDeclaration(D)) {
            C = D;
          }
          if (C && v.isIdentifier(C.name)) {
            var F = s.getSymbolAtLocation(C.name);
            if (F) {
              var k = g.get(C.name.text) || [];
              for (var N of k) {
                if (getRefSymbol(N) !== F) {
                  continue;
                }
                var _ = getVariableDeclaration(N);
                if (!_ || _ === C) {
                  continue;
                }
                trackBinding(_.name, A.root, A, !1);
              }
            }
          } else if (!C) {
            var O = getVariableDeclaration(w);
            if (O && v.isIdentifier(O.name)) {
              addEntry(s.getSymbolAtLocation(O.name), A.root, A, !1);
            }
          }
          var I = resolveDataType(w, s);
          if (I) {
            for (var L of m) {
              if (L === C) {
                continue;
              }
              var P = b.get(L);
              if (!P) {
                var V = unwrapAbstractType(s.getTypeAtLocation(L.initializer));
                P = {
                  type: V
                };
                if (V.flags & v.TypeFlags.Object) {
                  var z = V.getProperty("0");
                  if (z) {
                    P.tupleType = s.getTypeOfSymbol(z);
                  }
                  var R = (P.tupleType || V).getProperty("data");
                  if (R) {
                    P.dataType = unwrapAbstractType(s.getTypeOfSymbol(R));
                  }
                }
                b.set(L, P);
              }
              if (P.type === I || P.tupleType === I || P.dataType === I) {
                trackBinding(L.name, A.root, A, !1);
              }
            }
          }
        }
        for (var $ = 0; $ < x.length; $++) {
          var W = x[$];
          var j = g.get(W.symbol.name);
          if (!j) {
            continue;
          }
          for (var B of j) {
            if (getRefSymbol(B) !== W.symbol) {
              continue;
            }
            walkUseChain(B, W);
          }
        }
        var _loop2 = function(t) {
          if (!t.accessCount) {
            return 1;
          }
          var i = t.templateNode;
          var a = t.fieldToLoc;
          var s = t.allPaths.filter(e => !t.leafByPath.get(e).used);
          var l = new Set;
          var c = {};
          var h = new Set;
          s.forEach(e => {
            var t = e.split(".");
            t.pop();
            var i = t.join(".");
            if (a.get(i)) {
              l.add(i);
              if (c[i]) {
                c[i].add(e);
              } else {
                c[i] = new Set([ e ]);
              }
            } else {
              h.add(e);
            }
          });
          l.forEach(t => {
            var s = a.get(t);
            var l = c[t];
            n.push({
              file: e,
              length: s.length,
              start: i.getStart() + s.start + 1,
              category: v.DiagnosticCategory.Warning,
              code: Q,
              messageText: `Field(s) ${[ ...l ].map(e => `'${e}'`).join(", ")} are not used.`
            });
          });
          h.forEach(t => {
            var s = a.get(t);
            n.push({
              file: e,
              length: s.length,
              start: i.getStart() + s.start + 1,
              category: v.DiagnosticCategory.Warning,
              code: Q,
              messageText: `Field ${t} is not used.`
            });
          });
        };
        for (var M of h) {
          if (_loop2(M)) {
            continue;
          }
        }
      } catch (e) {
        console.error("[GraphQLSP]: ", e.message, e.stack);
      }
      return n;
    })(t, i.map(e => e.node), s) || [];
    if (!S) {
      return [ ...d, ...m ];
    }
    return [ ...d, ...m, ...S ];
  } else {
    return [ ...d, ...m ];
  }
};

export { ee as A, CharacterStream as C, bubbleUpTemplate as a, bubbleUpCallExpression as b, getSchemaName as c, getAllFragments as d, isGraphQLTag as e, findNode as f, getSource as g, findAllCallExpressions as h, isGraphQLCall as i, init as j, B as k, getPersistedCodeFixAtPosition as l, getGraphQLDiagnostics as m, findAllPersistedCallExpressions as n, onlineParser as o, print as p, getDocumentReferenceFromDocumentNode as q, resolveTemplate as r, getDocumentReferenceFromTypeQuery as s, v as t, unrollTadaFragments as u };
//# sourceMappingURL=api-chunk.mjs.map
